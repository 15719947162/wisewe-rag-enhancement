# TODO Ledger

> 共享任务账本。允许 `human`、`codex`、`claude`、`gsd` 共用。  
> 详细规则见：`docs/agent/agent-shared-docs.md`

---

## Active
> 2026-06-26 / T-189 / DONE / codex / docs-planning: 整体排查 Phase 11 现有实现与文档未对齐 / 未闭合问题，按当前代码事实收口 `docs/product/phase11-gap-analysis.md`、`docs/api/reference.md`、`docs/product/external-governance-integration-contract.md`、`docs/product/ai-base-identity-delta-sync-guide.md`、`docs/product/README.md`、`.planning/STATE.md`、11-01 / 11-04 phase summary。已统一真实 SSO `superManager` 触发 `http_delta` 全量同步、身份同步页全量明细/筛选/清空入口、OpenAPI 知识库/入库接口、OpenAPI app 生命周期、API Key HMAC/IP/rpm/daily 配额和脱敏审计的当前状态；剩余项明确为失败重试、生产关闭旧请求头兜底验收、访问拒绝审计全覆盖、历史知识库归属迁移检查、T-172 子资源权限收口、OpenAPI 并发/月度配额、SDK 和报表。验证：文档关键词残留检查通过；未运行代码测试（仅文档变更）。
> 2026-06-26 / T-188 / TODO / codex / feature / `backend/routes/` `backend/services/` `core/output/` `frontend/src/` `docs/api/reference.md` `.planning/phases/12-01-multi-source-ingestion/`：Phase 12 多来源入库入口扩展，支持文件文档、网页链接和系统备份 CSV 三种方式；保持现有 PDF 链路最小改动，网页采集与备份 CSV 快速导入作为新增适配层和恢复层，普通 CSV 不得绕过清洗/切片/质检/向量化；需同步控制台入口、任务台账和系统备份 CSV 版本化 schema。
> 2026-06-26 / T-187 / DONE / codex / frontend-backend: 调整“身份与权限同步”页：移除独立操作卡片，将刷新、立即同步、移除全部数据（二次确认）和查看日志放到表格右上方；用户明细新增租户名称、用户名称、原始角色、同步时间筛选；快照列表按最新成功同步用户数拉取全量，不再限制最多 200 条；RAG 角色显示为“RAG管理员”，原始角色列缩短并通过 tooltip 展示完整内容；新增 superManager 受控的本地身份快照清空接口并写入审计。验证：`frontend npm run typecheck`、`pytest tests/test_identity_access.py tests/test_backend_app.py -q`、`pytest tests/test_api_console.py -q`、`python -m py_compile backend/routes/identity.py core/db/identity.py` 通过。
> 2026-06-26 / T-186 / DONE / codex / frontend: 调整 OpenAPI 页面信息密度。API 总表新增“详情”列，点击“查看”后只展示当前选中 API 的详情面板，移除原先所有 API 详情同时堆叠在页面下方的布局；选中行使用浅色高亮，详情面板保留使用场景、鉴权方式、强签名、参数表、调用示例、成功响应和错误响应。验证：`frontend npm run typecheck` 通过。
> 2026-06-26 / T-185 / DONE / codex / feature: 使用 GSD quick 继续补齐 AI 基座 identity delta 用户状态联动。`/ai/system/internal/identity/snapshot/delta?last_sync_at={lastSyncAt}` 的用户对象现支持 `deleted` / `disabled` 位语义：`0` 视为健康，`1` 分别归一为 `deleted` / `disabled`；失效用户名下 `owner_user_id` 或历史 `created_by` 匹配的知识库会标记为 `owner_status='pending_transfer'`、`owner_invalid_reason='deleted|disabled'`，供管理员接管；新增管理员接口 `POST /api/knowledge-bases/{kb_id}/transfer-owner`，校验新归属人为同租户 active 用户，转移后恢复 `owner_status='active'` 并写入脱敏审计 `knowledge_base.transfer_owner`。验证：`python -m py_compile core/db/identity.py core/db/knowledge_base.py backend/services/kb_service.py backend/routes/knowledge_bases.py backend/schemas/requests.py` 通过；`pytest tests/test_identity_access.py tests/test_ai_base_identity_sync.py tests/test_backend_app.py -q` 66 passed。
> 2026-06-26 / T-183 / DONE / codex / feature: 使用 GSD quick 继续推进 OpenAPI app 生命周期与 API Key 治理控制台可操作性。新增 `/api/console/openapi-apps` 列表、创建、更新、删除接口，后端通过 `kb_openapi_apps` 管理调用方 app，并写入 `openapi_app.create/update/delete` 脱敏审计；前端 API Key 管理页新增调用方 app 创建、启停、删除与绑定选择，创建 API Key 时可填写 `appId`、`requireSignature`、`allowedIps`、`rpmLimit`、`dailyRequestLimit`，后端字段已贯通到控制台合同。验证：`python -m py_compile core/db/api_keys.py backend/services/console_service.py backend/routes/console.py tests/test_api_keys.py` 通过；`pytest tests/test_api_keys.py tests/test_openapi_v1.py tests/test_query_logs.py tests/test_api_console.py -q` 59 passed；`frontend npm run typecheck` 通过。
> 2026-06-26 / T-184 / DONE / codex / backend-docs: 使用 GSD quick 继续补齐 AI 基座用户端开放 API 后端最小闭环。新增并验证 `GET /openapi/v1/knowledge-bases`、`GET /openapi/v1/ingestion/options`、`GET /openapi/v1/ingestion/tasks/{task_id}` 与 `POST /openapi/v1/ingestion/upload`；知识库列表按 API Key 绑定范围裁剪，上传接口强制 OpenAPI HMAC 签名，清洗 / 质检提示词仅支持 append 并要求独立 capability。同步控制台 `/openapi` API 总表、`docs/api/reference.md`、`docs/product/phase11-gap-analysis.md`、11-04 summary 与 quick summary；当前上传签名 `X-KB-Body-SHA256` 按文件 bytes 计算，`parser_provider` 仅记录为请求元数据，真实解析管道仍由运行时 `PDF_PARSER_PROVIDER` 决定。验证 `python -m py_compile backend/routes/openapi_v1.py core/db/api_keys.py`、`pytest tests/test_openapi_v1.py tests/test_api_keys.py -q`、`frontend npm run typecheck` 通过。

> 2026-06-26 / T-182 / DONE / codex / feature: 使用 GSD quick 继续补齐 OpenAPI 治理闭环。已新增 API Key 级 `rpm_limit` / `daily_request_limit` 与 `kb_api_key_usage_windows` 分钟/日计数强制拦截，超限返回 `RATE_LIMITED` / `QUOTA_EXCEEDED`；新增 `kb_openapi_apps` 最小 app 模型表与 `kb_api_keys.app_id`，并将 `appId`、`rpmLimit`、`dailyRequestLimit` 贯通到控制台 API Key 创建/更新/列表 payload；OpenAPI API Key 鉴权失败现在写入脱敏 `openapi.auth_denied` 审计，覆盖签名、body hash、timestamp、nonce、IP、能力和知识库绑定拒绝等失败，不记录 API Key 明文、签名、body、query 或 answer；新增 `examples/openapi_signed_query.py` 作为 HMAC 签名 SDK 示例。验证：`python -m py_compile core/db/api_keys.py core/db/schema.py core/db/init_db.py backend/routes/openapi_v1.py backend/routes/console.py backend/services/console_service.py examples/openapi_signed_query.py tests/test_api_keys.py tests/test_openapi_v1.py` 通过；`pytest tests/test_api_keys.py tests/test_openapi_v1.py tests/test_query_logs.py tests/test_api_console.py -q` 58 passed。
> 2026-06-26 / T-181 / DONE / codex / frontend-docs: 使用 GSD quick 调整召回管理“历史问题”保留与分页策略。`QueryWorkspace` 的浏览器本地历史从最多 10 条改为最多 100 条，并接入 `TablePagination` 的 `compact` 简易分页，每页展示 20 条；历史读取增加类型容错，避免损坏的 `localStorage` 内容影响页面。同步 `docs/design/system/MASTER.md` 和 `docs/design/system/pages/online-rag.md`：窄侧栏可用简易分页承载入口历史，完整/审计级历史应进入后端分页日志。验证 `frontend npm run typecheck` 通过；见 `.planning/quick/260626-retrieval-history-pagination/SUMMARY.md`。

> 2026-06-26 / T-180 / DONE / codex / docs-frontend: 使用 GSD quick 完成 AI 基座用户端开放 API 调用文档产品化。控制台 `/openapi` 已从两个接口卡片改为“API 总表 + 点击展开单接口详情”，表格覆盖 API 名称、API 用途、方法路径、能力标识、强签名要求和当前状态；详情覆盖参数位置/类型/说明、调用示例、成功响应和错误响应。已明确 `/openapi/v1/rag/query` 与 `/openapi/v1/rag/graph-query` 为已开放，知识库列表、上传文件、入库任务详情、入库可选项、清洗提示词追加和质检提示词追加为规划待接入；上传、提示词追加和写操作必须强签名，查询类生产建议强签名且受 API Key `requireSignature` 约束。同步 `docs/api/reference.md`、`docs/product/phase11-gap-analysis.md` 和 11-04 summary。

> 2026-06-26 / T-179 / DONE / codex / feature: 使用 GSD quick 补齐 OpenAPI / API Key 强验证最小闭环。`kb_api_keys` 新增 `require_signature` 与 `allowed_ips`，新建 Key 默认要求强签名；新增 `kb_api_key_nonces` 记录同一 API Key 的短期 nonce，拦截重放；`/openapi/v1/rag/query` 与 `/openapi/v1/rag/graph-query` 读取原始 body 后校验 `X-KB-Timestamp`、`X-KB-Nonce`、`X-KB-Body-SHA256`、`X-KB-Signature`，签名串为 `METHOD\nPATH\nTIMESTAMP\nNONCE\nBODY_SHA256`，HMAC-SHA256 使用一次性明文 API Key，默认 5 分钟时间窗、10 分钟 nonce 保留；支持 API Key 级单 IP / CIDR 白名单并返回 `SIGNATURE_REQUIRED`、`BODY_HASH_MISMATCH`、`TIMESTAMP_EXPIRED`、`NONCE_REPLAYED`、`IP_NOT_ALLOWED` 等稳定错误码。已同步 `docs/api/reference.md`、`docs/product/phase11-gap-analysis.md` 和外部治理契约当前实现边界；QPS / quota / 并发强制、通用 app 模型、签名失败审计和 SDK 示例仍保留后续项。验证：`python -m py_compile core/db/api_keys.py core/db/schema.py core/db/init_db.py backend/routes/openapi_v1.py backend/routes/console.py backend/services/console_service.py` 通过；`pytest tests/test_api_keys.py tests/test_openapi_v1.py tests/test_query_logs.py tests/test_api_console.py -q` 49 passed。

> 2026-06-25 / T-178 / DONE / codex / bugfix: 修复“身份与权限同步”页缺少明细表的问题，并补齐 AI 基座增量同步 delta 字段兼容层。`/identity-monitor` 已从占位说明页改为真实表格，直接展示租户、用户、原始角色、RAG 角色、同步方式、同步时间；后端 `list_identity_snapshot_users()` 新增 `roleNames`、`ragRole`、`syncedAt`，`upsert_identity_delta_snapshot()` 兼容 snake_case / camelCase / `tenantList` / `userList` / `roleList` / `userRoles` / `deletedList` 等返回形态，避免因字段命名差异导致 tenants 有数而 users/roles 为 0。验证 `pytest tests/test_identity_access.py tests/test_backend_app.py tests/test_ai_base_sso.py tests/test_api_console.py -q` 97 passed，`frontend npm run typecheck` 通过。
> 2026-06-25 / T-177 / DONE / codex / feature: 收口 AI 基座用户及权限 delta 同步闭环，并按 2026-06-25 复核修正权限口径。`last_sync_at/max_updated_at` 已统一格式化为 `YYYY-MM-DD HH:mm:ss`，首次同步传空；日志管理新增“用户及权限同步”页签，同步成功后通过 `GET /api/console/identity-sync-logs?limit=100` 读取 `kb_identity_sync_runs` 展示租户、用户、角色、用户角色、删除事件数量和水位，并提供“立即同步”入口；左侧“身份与权限同步”菜单已从旧请求头 / 临时快照主口径改为 SSO + 同步日志 + 访问治理状态入口。五类 delta 入库的 `POST /api/identity/sync-delta` 已收紧为仅允许 AI 基座 SSO session 来源且 `roleCodes` 包含 `superManager` 的当前身份执行；平台管理员、普通用户和旧 `X-KB-*` 请求头身份均不能触发五类入库。T-176 的后台 scheduler 口径已随本轮收紧为无当前 SSO `superManager` 上下文时只记录 `skipped`，不直接执行五类入库。二次防遗漏复核已把 `docs/product/ai-base-identity-delta-sync-guide.md`、`docs/product/ai-base-sso-integration-guide.md` 和产品索引中的旧 `cursor/next_cursor` 合同统一为 `last_sync_at/max_updated_at`，并将后端旧 `next_cursor` 兼容收紧为不推进水位；本轮继续闭合 `docs/product/knowledge-base-governance-brd.md`、`docs/product/phase11-gap-analysis.md`、`.planning/REQUIREMENTS.md` 和 11-01 `PLAN/CONTEXT` 中的“仅 SSO `superManager` 可触发”、用户及权限同步日志、`kb_identity_sync_runs` 数据源口径。运行库复核：`kb_identity_sync_runs` 当前仍只有历史 `mysql_bootstrap`，尚无 `http_delta`，需要真实 SSO `superManager` 点击日志页“立即同步”或调用 `POST /api/identity/sync-delta` 后才会产生租户侧同步日志。验证 `pytest tests/test_ai_base_sso.py tests/test_api_console.py tests/test_identity_access.py -q` 52 passed，`frontend npm run typecheck` 通过。
> 2026-06-24 / T-176 / DONE / codex / feature-docs: 补齐 Phase 11 身份同步调度状态与生产兜底开关最小切片。新增 `KB_LEGACY_HEADER_AUTH_ENABLED`，生产切正式 SSO 后可关闭旧 `X-KB-*` 请求头身份兜底；新增 `AI_BASE_IDENTITY_SYNC_ENABLED`、`AI_BASE_IDENTITY_SYNC_INTERVAL_SECONDS`、`AI_BASE_IDENTITY_SYNC_RUN_ON_STARTUP` 和后台 scheduler 状态观测，但按最新安全边界，后台任务没有当前 SSO `superManager` 上下文时只记录 `skipped`，不直接调用 AI 基座 HTTP delta 入库；新增管理员只读 `GET /api/identity/sync-status` 观测调度状态与水位；同步 env 模板、控制台设置分组和 `docs/product/phase11-gap-analysis.md`。验证 `python -m py_compile backend/services/identity_sync_scheduler.py backend/services/identity_service.py backend/routes/identity.py backend/app.py core/runtime_settings.py backend/services/console_service.py` 通过，`pytest tests/test_ai_base_sso.py tests/test_identity_access.py tests/test_api_console.py -q` 46 passed。
> 2026-06-24 / T-175 / DONE / codex / verification-docs: AI 基座放行后复核 Phase 11 SSO 与身份同步闭合状态。运行时日志确认 `/api/auth/ai-base/launch` -> AI 基座 `/sso` -> `/api/auth/ai-base/callback` -> `/api/auth/session` 已成功，`kb_auth_sessions` 已写入 `auth_source=ai_base_sso_authorization_code`、`tenant_id=1`、`user_id=100`、`identity_snapshot_version` 的本地短 session；SSO 登录与当前登录用户摘要落快照可按联调口径闭合。未发现运行时调用 `POST /api/auth/ai-base/refresh-current-user` 或 `POST /api/identity/sync-delta`，`kb_identity_sync_runs` 仍只有 2026-06-18 的 `mysql_bootstrap`，没有 `http_delta` 记录；因此身份 HTTP delta 仍停留在“接口能力已实现，但尚未由真实 SSO `superManager` 触发入库”的状态，后台调度不具备该上下文时只会 `skipped`。已同步 `docs/product/phase11-gap-analysis.md`。
> 2026-06-24 / T-174 / DONE / codex / feature: 补齐 Phase 11 Token 用量治理硬化切片。`kb_llm_call_logs` 已继续接入入库 clean / quality、在线问答 rerank LLM check / generation、评测打分和 OpenAPI 普通问答 domain；新增 `kb_token_usage_hourly` 增量 upsert 与窗口重建 helper，`/api/console/token-usage` 返回 `hourlyUsage`、费用估算、quota 状态和告警；前端 `/usage` 展示小时趋势、费用摘要和 quota 告警；查询日志 CSV 导出写入 `query_logs.export` 脱敏审计事件。托管解析 provider 与 Graph RAG 内部 LLM 逐次采集未验证，继续保留为后续项。验证 `pytest tests/test_query_logs.py tests/test_api_console.py tests/test_backend_app.py tests/test_openapi_v1.py tests/test_api_keys.py tests/test_identity_access.py -q` 97 passed，`frontend npm run typecheck` 通过；已同步 `docs/product/phase11-gap-analysis.md`。
> 2026-06-23 / T-173 / DONE / codex / bugfix: 修复 Token 统计页切换链路 Tab 后下方功能环节表仍显示全部链路的问题；后端 `/api/console/token-usage?pipeline_domain=...` 现在按当前链路裁剪预期环节占位，并按 `pipelineStage + featureName` 归组明细，避免在线问答 / Graph RAG / OpenAPI / 评测 Tab 混入入库链路的 chunk、embedding 等占位。验证 `pytest tests/test_query_logs.py -q` 12 passed，`frontend npm run typecheck` 通过。
> 2026-06-23 / T-169 / DONE / codex / feature-docs: 将 AI 基座 SSO 真实接口路径配置化并纳入控制台设置分组；新增 `AI_BASE_SSO_LAUNCH_BASE_URL`、`AI_BASE_SSO_LAUNCH_PATH`、`AI_BASE_SSO_EXCHANGE_PATH`、`AI_BASE_SSO_USER_SNAPSHOT_PATH_TEMPLATE`、`AI_BASE_SSO_DELTA_PATH` 的 runtime setting / env 模板 / `/api/auth/ai-base/config` 输出，明确 RAG launch 跳向的 AI 基座浏览器 SSO 端点与 RAG 后端调用的 AI 基座内部接口根地址分离。已补 SSO 配置与 `Result.data` 解包回归测试；`users/{userId}` 与 `delta` 的 HTTP 同步已由 T-170 补齐。
> 2026-06-23 / T-164 / DONE / codex / planning-docs: 使用 GSD quick 复核 AI 基座租户端 SSO 文档与当前 `.env` / 代码实现差异；当时确认 `.env` 已具备 AI 基座 base URL、`rag-client`、后端 8001 callback、控制台 3000 base、4 小时本地 session 和 CORS origin，并识别出真实 SSO 路径、launch `client_id` 参数、`Result.data` 解包、`last_sync_at/max_updated_at` HTTP delta 同步和当前用户刷新等待补点。后续已由 T-169/T-170 补齐，并在 2026-06-24 收敛为 RAG `/api/auth/ai-base/launch` 统一生成 state/cookie 后跳转 AI 基座 `/sso`。
> 2026-06-23 / T-163 / DONE / codex / feature: 使用 GSD quick 完成日志分层与审计日志最小切片。新增 `kb_audit_logs` schema、审计写入 / 查询 helper 和 `/api/console/audit-logs`；API Key 创建 / 更新 / 轮换 / 删除与配置保存成功后写入脱敏审计事件；`/logs` 页面拆为查询日志、入库日志、审计日志三类视图，模型调用明细继续归属 Token 统计页；按用户澄清，本轮不强化查询日志 CSV 导出，已确认 CSV 产品口径仍是文档切片入库后导出。验证 `pytest tests/test_query_logs.py tests/test_api_console.py -q` 30 passed，`frontend npm run typecheck` 通过；见 `.planning/quick/260623-audit-log-layering/SUMMARY.md`。
> 2026-06-23 / T-162 / DONE / codex / feature-bugfix: 使用 GSD quick 完成入库管理产品化与 Token 统计补点。`/ingestion` 改为可检索分页的大表格台账，展示知识库名称/ID、文档信息、切片策略、创建/更新时间、操作用户和阶段耗时，“查看”弹窗展示单环节耗时与 metrics；运行中任务改为友好遮罩，不再主界面实时刷日志；新增最新一次入库日志接口和展示区；Token 统计页按链路 Tab 展示，后端从 `kb_llm_call_logs` 聚合并支持 `pipeline_domain` 过滤，入库 chunk / embedding 阶段会写入 LLM 调用明细，修复三层切片增强 Token 后续不统计的问题。二次验收补齐遗漏：上传请求期间新增“文件正在上传中...”遮罩，单库入库工作台移除 SSE 实时日志面板并对齐默认端口 `8001`。验证 `pytest tests/test_query_logs.py tests/test_api_console.py tests/test_backend_app.py -q` 67 passed，二次复核 `pytest tests/test_api_console.py tests/test_query_logs.py -q` 30 passed，`frontend npm run typecheck` 与 `frontend npm run build` 通过；见 `.planning/quick/260623-ingestion-management-productization/SUMMARY.md`。
> 2026-06-23 / T-161 / DONE / codex / bugfix-ops: 修复控制台跨域预检报 `x-kb-user-id is not allowed`。后端 CORS 改为显式允许 `X-KB-Tenant-Id` / `X-KB-User-Id` / `X-API-Key` 等请求头并支持 `KB_CORS_ALLOW_HEADERS` 覆盖；补充预检回归测试；发现宿主机 `8000` 实际返回 C-Lodop 打印服务页面，Docker 后端对外端口改为 `8001`，前端 `NEXT_PUBLIC_API_BASE_URL` 与 SSO 回调同步改到 `http://192.168.2.208:8001`；已重建 backend/frontend 容器。验证 `pytest tests/test_backend_app.py -q` 40 passed，`docker compose up -d --build backend frontend` 构建通过，`http://192.168.2.208:8001/api/knowledge-bases` OPTIONS 预检返回允许身份头。
> 2026-06-23 / T-160 / DONE / codex / bugfix: 使用 GSD quick 修复随机 24 位 hex 知识库 ID 下单库页面打不开；单库 layout、总览、文档、入库、问答、评测、图谱页均改为直接 decode 路由 ID，不再服务端拉取全量知识库列表并反查 ID；总览和图谱页对文档 / 评测 / 图谱接口失败增加空数据兜底，避免新建空库或短暂接口异常触发 `This page couldn’t load`；删除前端旧 `resolveKnowledgeBaseId` helper，并补 `/api/documents`、`/api/ingestion/tasks`、`/api/console/evaluations` 的 hex ID 过滤回归测试。验证 `frontend npm run typecheck`、`frontend npm run build`、`pytest tests/test_backend_app.py tests/test_api_console.py -q` 均通过；见 `.planning/quick/260623-hex-kb-id-workspace-routing/SUMMARY.md`。
> 2026-06-23 / T-159 / DONE / codex / bugfix-docs: 使用 GSD quick 完成全项目乱码高置信复扫与修复；清理后端解析进度 `_ParseStageTracker` 中残留的损坏兼容字符串、RAG enhanced 折回评分中的损坏标记、`tests/test_generator.py` 损坏测试夹具、`data/results/console_evaluations.json` 问号占位 query、`todo.md` T-043 历史占位行，并将大面积损坏的 `bug.md` 重建为可读共享账本。验证高置信乱码扫描 `HIGH_CONFIDENCE_FILES=0`；相关编译和测试见 `.planning/quick/260623-project-mojibake-sweep/SUMMARY.md`。
> 2026-06-23 / T-158 / DONE / codex / frontend-docs: 使用 GSD quick 完成控制台前端可见乱码整体清理；恢复全局导航壳层、知识库列表、系统总览、ContextRail、API Key、日志管理、OpenAPI、全局入库、问答、评测、单库总览/布局/图谱/评测以及治理占位、告警栈、切片树等共享组件中文文案；同步 `.planning/quick/260623-frontend-mojibake-repair/`。验证 `frontend npm run typecheck` 通过，且 `frontend/src` 未发现 Unicode 替换符或私用区字符残留。历史账本/文档中的旧乱码暂不大段重写。
> 2026-06-22 / T-157 / DONE / codex / frontend-backend-docs: 使用 GSD quick 完成 Token 统计按功能环节明细化补丁；新增 `kb_llm_call_logs` schema，`/api/console/token-usage` 返回 `pipelineStages` / `llmCalls` / `detailAvailable` / `chartReady=false`，超级管理员可查看所有租户、知识库和环节的消耗明细，普通身份按租户过滤；前端 `/usage` 改为环节展开和模型调用明细视图，明确未接入明细采集的环节；同步 `.planning/quick/260622-token-usage-stage-details/`、Phase 11 摘要、API 文档和缺口分析。验证 `pytest tests/test_query_logs.py tests/test_api_console.py -q` 24 passed，`frontend npm run typecheck` 通过。
> 2026-06-22 / T-156 / DONE / codex / frontend-docs: 按“AI 基座统一登录入口 + 知识库本地短 session”方案收口退出语义；控制台按钮从“退出登录”改为“退出知识库”，默认仅清理 RAG 本地 `kb_session` 并跳转 `/login?logged_out=1`；登录页展示“已退出知识库”提示和“使用 AI 基座重新登录”入口；同步 `docs/product/ai-base-sso-integration-guide.md` 明确默认不退出 AI 基座账号，若需全局退出须独立接 AI 基座统一 logout。
> 2026-06-22 / T-155 / DONE / codex / frontend: 优化配置中心产品化展示；降低整页高饱和常亮配色，改为克制白底设置台；配置项主标题使用友好中文名称，原始环境变量 / 配置 key 降级为辅助技术标识；来源标识改为控制台覆盖、环境变量、配置文件、内置默认、系统状态；仅当前分组、编辑态、未保存修改和保存操作保持明确视觉重点。验证 `frontend npm run typecheck` 与 `frontend npm run build` 通过。
> 2026-06-22 / T-154 / DONE / codex / frontend-docs: 使用 GSD quick 收口全局卡片视觉简化标准；内容卡片移除顶部彩条，统一改为浅渐变、细边框、轻阴影和右下角柔和色块；左侧菜单保持扁平导航，不套卡片模式；表格分页维持默认 20 条、可选 20 / 50 / 100、控件靠右；同步 `docs/design/ui-interaction-standard.md` 与 `docs/design/previews/wisewe-rag-console-ui-preview.html`，并清理中断残留的错误 Tailwind class。验证 `frontend npm run typecheck` 与 `frontend npm run build` 通过。
> 2026-06-22 / T-153 / DONE / codex / frontend: 使用 GSD quick 为前端真实业务表格和长列表补齐统一分页；新增 `TablePagination` / `useClientPagination`，默认每页 20 条，可选 20 / 50 / 100，分页控件靠右；覆盖 API Key、日志、Token 用量、全局/单库评测、文档列表、系统总览队列/最近任务、全局/单库入库队列、待确认切片草稿和工作台证据表；静态 OpenAPI 说明表格不强行分页。验证 `frontend npm run typecheck` 与 `frontend npm run build` 通过。

> 2026-06-22 / T-152 / DONE / codex / frontend: 修正左侧主菜单过度卡片化问题：移除菜单分组外层卡片边框、底色、圆角和阴影，恢复为产品控制台常见的扁平分组导航；保留分组彩色识别点、业务信号文案、彩色图标、hover 与 active 识别态，避免把内容卡片规范生搬硬套到导航结构。验证 `frontend npm run typecheck` 通过。
> 2026-06-22 / T-151 / DONE / codex / frontend: 按用户反馈修正控制台壳层与总览身份区布局：主内容和头部容器取消 `max-w-[1440px]` 居中限制并收紧左右 padding，减少宽屏左右留白；当前身份信息移动到 `/overview` 的“系统总览”视觉模块上方，仅展示身份与租户上下文；“切换身份”和“退出登录”统一放到顶部 header 右侧，身份卡片内不再放操作按钮。验证 `frontend npm run build` 与 `frontend npm run typecheck` 通过。

> 2026-06-22 / T-150 / DONE / codex / frontend: 将当前身份信息从左侧菜单列移入系统总览模块。侧栏和顶部 header 不再展示身份详情，左侧仅保留导航；`/overview` 新增“当前身份”卡片，展示显示名、租户、用户名、用户 ID、角色编码、身份来源，并提供“切换身份”和“退出登录”操作。验证 `frontend npm run typecheck` 与 `frontend npm run build` 通过。

> 2026-06-22 / T-149 / DONE / codex / frontend-docs: 调整控制台主菜单为用户指定的新分组：控制台/系统总览，权限管理/身份与权限，知识库管理/知识库与入库管理，召回与评测管理/召回管理与评测记录，配置管理/配置中心，API管理/API Key 与 OpenAPI，日志与统计管理/日志管理与 Token统计；主菜单移除“离线入库”入口但保留历史兼容路由；同步页面标题、单库 Tab 和 `docs/design` 设计规范。

> 2026-06-22 / T-148 / DONE / codex / frontend: 为数据量较大的前端页面补充 loading 过渡动画。新增通用 shimmer skeleton、顶部加载进度条、半透明刷新覆盖层与数据进入动画；接入日志中心、Token 用量、知识库列表、系统总览、系统配置、API Key、全局/单库评测、文档列表与文档详情弹窗，避免刷新或大数据加载时页面空白、误显空态或明显跳动。验证 `frontend npm run typecheck` 与 `frontend npm run build` 通过。

> 2026-06-22 / T-147 / DONE / codex / ops: 补齐 AI 基座 SSO 联调环境变量配置。已在根 `.env`、`.env.example`、`.env.docker.example` 增加 `AI_BASE_SSO_BASE_URL`、`AI_BASE_SSO_CLIENT_ID`、`AI_BASE_SSO_CLIENT_SECRET`、`AI_BASE_SSO_REDIRECT_URI`、`KB_CONSOLE_BASE_URL`、`KB_SESSION_TTL_SECONDS`、`KB_CORS_ALLOW_ORIGINS` 及用途注释；`frontend/.env.local.example` 明确前端只配置浏览器 API base，SSO secret 仅保留在后端环境变量中。

> 2026-06-22 / T-146 / DONE / codex / planning: 按历史实现口径归档 Phase 10 计划目录。已将 `.planning/phases/10-*` 移动到 `.planning/milestones/v6.0-phases/`，并同步 `.planning/phases/README.md`、`.planning/milestones/README.md`、`.planning/ROADMAP.md` 与 `.planning/STATE.md`；后续 `.planning/phases/**` 仅保留当前 v7.0 Phase 11 主线计划。

> 2026-06-22 / T-145 / DONE / codex / feature: 使用 GSD quick 实现 RAG 侧 AI 基座 SSO exchange 与知识库本地短 session 闭环。新增 `kb_auth_sessions`、`kb_sso_used_credentials`、`/api/auth/ai-base/config|launch|callback|exchange`、`/api/auth/session`、`/api/auth/logout`；身份解析优先使用 HttpOnly `kb_session` cookie，旧 `X-KB-Tenant-Id` / `X-KB-User-Id` 仅保留为开发联调兜底；前端登录页接入正式 SSO、JWT 一次性交换和本地快照 fallback，API/upload/export/query/SSE 均携带 cookie；同步 `docs/product/phase11-gap-analysis.md`、`docs/product/ai-base-sso-integration-guide.md` 与 `.planning/quick/260622-ai-base-sso-session/`。验证通过：`python -m py_compile core/db/identity.py backend/services/identity_service.py backend/services/ai_base_sso_service.py backend/routes/identity.py backend/app.py`；`pytest tests/test_ai_base_sso.py tests/test_identity_access.py tests/test_db_init.py tests/test_backend_app.py -q` 53 passed；`pytest tests/test_api_keys.py tests/test_openapi_v1.py tests/test_query_logs.py tests/test_api_console.py -q` 32 passed；`frontend npm run typecheck`。

> 2026-06-22 / T-144 / DONE / codex / frontend: 使用 GSD quick 按 `docs/design/ui-interaction-standard.md` 与 `docs/design/previews/` 对 `frontend/` 控制台完成全前端 UI 与交互产品化重修；此前临时 UI 方案全部作废，本轮以规范预览为唯一视觉依据。覆盖全局科技浅背景、业务域侧栏菜单、基础 Card/Metric/Badge、知识库指标卡、`/overview`、知识库列表、单库总览/文档/入库/问答/评测/图谱、全局入库/问答/评测、API Key、OpenAPI、日志、用量、设置、身份治理占位页、登录入口与高频工作台组件；按预览补齐多色浅渐变、域色边框、4px 顶部色带、右下角色块、浅 tint 表头和稳定 hover/focus，保留输入框、代码块、Modal 与正文阅读面白底高对比；验证 `frontend npm run typecheck` 与 `frontend npm run build` 通过。

> 2026-06-22 / T-141 / DONE / codex / docs: 按 AI 基座采用 JWT 认证机制的事实，细化正式 SSO 对接文档。`docs/product/ai-base-sso-integration-guide.md` 从单一 `sso_code` 契约扩展为“推荐 JWT 内部认证 + 一次性 sso_code broker，兼容 JWT 一次性交换 / 校验”的双模式说明，补齐 JWT/JWKS、公钥轮换、`iss/aud/exp/nbf/iat/kid/jti` 校验、JWT 传递与存储限制、知识库本地短 session 边界、OpenAPI Bearer API Key 与 AI 基座 JWT 隔离、错误码、审计字段和联调清单；同步 `docs/product/external-governance-integration-contract.md`、`docs/product/phase11-gap-analysis.md`、`docs/product/README.md` 与 `docs/api/reference.md`。本次仅文档变更，未改服务端逻辑。
> 2026-06-22 / T-142 / DONE / codex / docs: 将 AI 基座身份快照增量同步方案整理为面向 AI 基座租户端开发的详细对接文档 `docs/product/ai-base-identity-delta-sync-guide.md`，覆盖 cursor 语义、定时增量同步、登录时兜底刷新、delta 接口、当前用户刷新接口、状态探测 / webhook 可选能力、必须返回的租户 / 用户 / 角色 / 用户角色关系变更类型、删除 / 停用 / 角色移除检测、字段规范、变更检测规则、知识库侧落库建议、事务重试、快照新鲜度、管理员判断、安全要求、错误码和联调验收清单；同步 `docs/product/ai-base-sso-integration-guide.md` 与 `docs/product/README.md`。本次仅文档变更，未改服务端逻辑。
> 2026-06-22 / T-143 / DONE / codex / docs: 补充 OpenAPI / API Key 的通用调用方边界，明确 AI 基座只是可接入调用方之一，后续第三方系统也应复用同一 app/API Key/HMAC/IP 白名单/quota/审计模型，不得把 `/openapi/v1/*`、API Key 字段、签名协议或 quota 设计写死为 AI 基座专用能力；同步 `docs/product/ai-base-sso-integration-guide.md`、`docs/product/external-governance-integration-contract.md`、`docs/api/reference.md`、`docs/product/phase11-gap-analysis.md` 和共享账本。本次仅文档变更，未改服务端逻辑。
> 2026-06-21 / T-140 / DONE / codex / docs: 使用 GSD quick 补齐 Phase 11 外部治理能力对接契约。新增 `docs/product/external-governance-integration-contract.md`，覆盖正式 AI 基座 SSO、身份快照、OpenAPI HMAC 强签名、timestamp / nonce / body hash、IP 白名单、quota / QPS / 并发限制、审计日志、配置版本 / 回滚和失效用户接管；文档明确这是目标对接契约，当前代码仍只实现临时身份、身份快照、本地 API Key 生命周期 MVP、OpenAPI 基础 Bearer / `X-API-Key`、脱敏查询日志和基础 token 聚合。同步产品文档入口、文档地图、API 参考、Phase 11 缺口清单和 `.planning/quick/260621-external-governance-integration-docs/`；本次仅文档变更，未改代码。
> 2026-06-21 / T-139 / DONE / codex / docs: 使用 GSD quick 梳理 Phase 11 当前未实现或延后的关键点，并补齐项目 HTTP API 参考文档。新增 `docs/product/phase11-gap-analysis.md`，按 11-01~11-06 区分已落地、快速贯通、未实现 / 延后、风险和优先级；新增 `docs/api/reference.md`，基于当前 FastAPI OpenAPI schema 与路由源码覆盖 `/api/*`、`/openapi/v1/*`、认证方式、请求参数、响应 / 错误约定和示例；同步 `docs/product/README.md`、`docs/document-map.md` 与 `.planning/quick/260621-phase11-gap-api-docs/`。本次仅文档变更，未改代码；正式 SSO、API Key 强签名/IP/nonce、完整审计、`kb_llm_call_logs`、小时级 token rollup、配置回滚等仍按 T-134 延后。
> 2026-06-21 / T-138 / DONE / codex / feature: 使用 GSD quick 补齐日志中心筛选导出独立切片。`/api/console/query-logs` 新增时间范围筛选，新增 `/api/console/query-logs/export.csv` 按当前筛选导出 UTF-8 BOM CSV；导出字段仅包含 `kb_rag_query_logs` 已脱敏的 requestId、租户 / 操作者 / 知识库 / API Key ID、pipeline、queryHash、摘要、token、延迟、评分、状态和时间，不导出完整 query、answer、prompt、文档正文、provider 原始响应或 API Key 明文。前端 `/logs` 增加开始 / 结束时间和“导出 CSV”操作。正式 SSO/session 授权、导出审计日志、全量审计日志体系和导出文件 24 小时清理仍按 T-134 延后。验证通过：`python -m py_compile core/db/query_logs.py backend/services/console_service.py backend/routes/console.py`，`pytest tests/test_query_logs.py tests/test_api_console.py -q` 20 passed，`pytest tests/test_api_keys.py tests/test_openapi_v1.py tests/test_query_logs.py tests/test_api_console.py -q` 32 passed，`frontend npm run typecheck`。

> 2026-06-20 / T-137 / DONE / codex / feature: 使用 GSD quick 补齐不依赖正式 AI 基座 SSO 的本地 API Key 生命周期 MVP。新增 `kb_api_keys` schema 与 `core/db/api_keys.py`，API Key 只保存 SHA-256 hash、前缀 / 后缀、状态、绑定知识库、能力范围和过期时间；创建与轮换仅在响应中返回一次明文。新增 `/api/console/api-keys` 创建、列表、启用 / 禁用、轮换和软删除接口，前端 `/api-keys` 从占位页升级为真实管理页；OpenAPI v1 支持 `Authorization: Bearer <api_key>` / `X-API-Key` 基础鉴权，并校验 `kb_id` 绑定和 `rag.query` / `rag.graph_query` 能力范围。强签名、timestamp、nonce 防重放、IP 白名单和配额强制拦截仍按 T-134 延后。验证通过：`python -m py_compile core/db/api_keys.py backend/routes/console.py backend/routes/openapi_v1.py core/db/query_logs.py core/db/schema.py core/db/init_db.py`，`pytest tests/test_api_keys.py tests/test_api_console.py tests/test_openapi_v1.py tests/test_query_logs.py tests/test_db_init.py -q` 32 passed，`frontend npm run typecheck`。

> 2026-06-20 / T-136 / DONE / codex / feature: 调整新建知识库 ID 策略，停止用知识库名称 / 中文拼音生成公开 ID，改为 24 位小写十六进制随机 ID，例如 `6a30fe65b0b256647e733f4b`。历史知识库 ID 和 `default` 默认库保持兼容不迁移；移除不再使用的 `pypinyin` 依赖；新增 `_make_kb_id()` 与 `POST /api/knowledge-bases` 回归测试，确保中文名称不会再生成拼音 slug。验证通过：`python -m py_compile backend/routes/knowledge_bases.py`，`pytest tests/test_backend_app.py tests/test_identity_access.py -q` 45 passed，`frontend npm run typecheck`。

> 2026-06-19 / T-135 / DONE / codex / feature: 使用 GSD quick 补齐不依赖正式 AI 基座 SSO 的独立治理模块。新增 `/api/console/query-logs` 与 `/api/console/token-usage`，分别提供脱敏 RAG 查询日志列表和基于 `kb_rag_query_logs` 的 token 聚合；前端 `/logs`、`/usage`、`/openapi` 已从占位页升级为真实日志中心、Token 用量统计和 OpenAPI v1 调用文档，文案明确 API Key 生命周期、签名、nonce、IP 白名单、LLM 调用明细表和小时级 rollup 尚未完成。验证通过：`python -m py_compile core/db/query_logs.py backend/services/console_service.py backend/routes/console.py`，`pytest tests/test_query_logs.py tests/test_api_console.py -q` 17 passed，`frontend npm run typecheck`。同步 `.planning/quick/260619-independent-governance-modules/260619-independent-governance-modules-SUMMARY.md`。

> 2026-06-19 / T-134 / TODO / shared / feature: 补齐 v7.0 知识库治理 BRD 与 phase11 最小切片之间的剩余实现缺口。重点包括正式 SSO code exchange / session / 统一访问判断中间件、API Key 生命周期与签名/IP/nonce/能力范围、日志中心筛选导出、`kb_llm_call_logs` 与小时级 token 聚合、配置回滚 / Prompt Profile / 灰度，以及失效用户接管转交等治理闭环；当前前端已先补 BRD 功能域导航和建设中入口，后续应按功能域逐项落地真实能力。
> 2026-06-19 / T-133 / DONE / codex / feature: 对齐 `docs/product/knowledge-base-governance-brd.md` 第 9 章功能域划分，重组控制台主导航为监控报表、知识库业务、文档与入库、在线问答、图谱与评测、开放接口、配置治理、日志与用量等功能域；新增 `identity-monitor`、`api-keys`、`openapi`、`logs`、`usage` 治理入口页，用“已落地底座 / 待补齐能力”明确 phase11 当前只是最小切片，避免把 API Key、日志中心、Token 统计等延后能力误认为已完整实现。验证 `frontend npm run typecheck` 通过。
> 2026-06-19 / T-132 / DONE / codex / feature: 补全 AI 基座 SSO 延后期间的前端验证入口。新增只读 `/api/identity/snapshot-users`，从本地 AI 基座身份快照返回脱敏后的租户 / 用户 / 角色样例；新增 `/login` 临时联调登录页，选择身份后写入浏览器本地状态；控制台壳层无身份时跳转登录，并显示当前身份、切换身份和退出入口；前端 API client、上传、CSV / 源文件下载和 RAG 查询直接 fetch 均注入 `X-KB-Tenant-Id` / `X-KB-User-Id`，用于从前端跑通 v7.0 11-01~11-06 治理链路。验证通过：`python -m py_compile core/db/identity.py backend/routes/identity.py backend/app.py`、`pytest tests/test_identity_access.py tests/test_backend_app.py -q` 43 passed、`frontend npm run typecheck`；真实 DB 复核暂因本机 PostgreSQL `localhost:5432` 未启动而未执行。

> 2026-06-18 / T-131 / DONE / codex / feature: 执行 v7.0 计划 11-06 最小切片：新增 `kb_rag_query_logs` 查询日志表与 `core/db/query_logs.py` 写入模块，RAG / Graph RAG 返回并贯穿 `requestId`，默认仅记录 `query_hash`、短摘要、租户 / 操作者 / 知识库 / pipeline domain、延迟、候选数、引用数和 token 元数据，不保存完整 query、answer、prompt 或文档正文；OpenAPI v1 可传入外层 requestId，日志写入 best-effort 不阻断主查询。已执行 PostgreSQL 初始化并确认关键列存在；验证 `pytest tests/test_query_logs.py tests/test_identity_access.py tests/test_openapi_v1.py tests/test_backend_app.py tests/test_rag_retriever.py tests/test_graph_retriever.py -q` 61 passed。同步 `.planning/phases/11-06-logs-monitoring-usage/SUMMARY.md`。

> 2026-06-18 / T-130 / DONE / codex / feature: 执行 v7.0 计划 11-05 最小切片：配置中心返回 `configScope`、`effectiveMode` 与 `governance` 元数据，初步标记热生效 / 灰度 / 运维变更边界；新增 `sanitize_console_settings_update()`，敏感配置项回传 `****xxxx` 脱敏展示值时不再被误保存为真实配置，保留保存失败显式 503。验证 `pytest tests/test_api_console.py tests/test_runtime_settings.py tests/test_backend_app.py -q` 54 passed。同步 `.planning/phases/11-05-configuration-governance/SUMMARY.md`。
> 2026-06-18 / T-129 / DONE / codex / feature: 执行 v7.0 计划 11-04 最小切片：新增 `/openapi/v1/*` 路由边界和查询类协议壳，挂载 `/openapi/v1/rag/query`、`/openapi/v1/rag/graph-query`；统一 `requestId` 与 `{error:{code,message,details}}` 错误格式；`kb_id` 缺失返回 `KB_ID_REQUIRED`，未认证返回 `API_KEY_REQUIRED`，未知字段如 `system_prompt` 被 schema 拒绝；快速贯通阶段临时复用 11-01 请求头身份，正式 API Key 生命周期、hash 存储、强签名、nonce/IP 白名单延后。验证 `pytest tests/test_openapi_v1.py tests/test_identity_access.py tests/test_backend_app.py -q` 45 passed。同步 `.planning/phases/11-04-openapi-api-key/SUMMARY.md`。
> 2026-06-18 / T-128 / DONE / codex / feature: 执行 v7.0 计划 11-03 最小切片：普通 RAG 与 Graph RAG 入口接入 11-01 身份上下文，在进入召回前通过知识库访问过滤校验 `kb_id`；跨租户、非归属、无权或已软删除知识库返回 404，避免通过猜测 `kb_id` 召回不可见内容。匿名路径暂保留兼容。验证 `pytest tests/test_identity_access.py tests/test_backend_app.py tests/test_rag_retriever.py tests/test_graph_retriever.py -q` 53 passed；真实 API 验证已软删除临时库对 `/api/rag/query` 与 `/api/rag/graph-query` 均返回 404。同步 `.planning/phases/11-03-rag-graph-evaluation-governed/SUMMARY.md`。
> 2026-06-18 / T-127 / DONE / codex / feature: 执行 v7.0 计划 11-02 最小切片：为 `knowledge_bases` 增加 `tenant_id`、`created_by`、`owner_user_id`、转交字段、`status`、`deleted_at` 与索引；创建知识库时写入当前租户和归属人；列表、详情、更新、删除接入服务端访问过滤，普通用户仅本人归属可见，`superManager` 租户管理员可见本租户全量；删除改为软删除并默认从列表隐藏。已执行真实 API 头部烟测，创建、列出、软删除和删除后不可见均通过；验证 `pytest tests/test_identity_access.py tests/test_ai_base_identity_sync.py tests/test_db_init.py tests/test_backend_app.py -q` 45 passed。同步 `.planning/phases/11-02-kb-business-loop/SUMMARY.md`。
> 2026-06-18 / T-126 / DONE / codex / feature: 执行 v7.0 计划 11-01 访问判断补齐：新增 `core/db/identity.py` 与 `backend/services/identity_service.py`，快速贯通阶段先从 `X-KB-Tenant-Id`、`X-KB-User-Id` 读取请求身份，再用本地 AI 基座只读快照确认租户、用户和 `role_code=superManager` 管理员标记；SSO code exchange 继续延后但入口已预留。实测样本 `tenant_id=1/user_id=1` 解析为 `systemAdmin` 且命中租户管理员；验证 `pytest tests/test_identity_access.py tests/test_ai_base_identity_sync.py tests/test_db_init.py tests/test_backend_app.py -q` 45 passed。追加更新 `.planning/phases/11-01-identity-access-foundation/SUMMARY.md`。
> 2026-06-18 / T-125 / DONE / codex / feature: 使用 GSD 执行 v7.0 最新计划的 11-01 快速贯通版。按用户要求将 SSO code exchange 暂缓，优先打通 AI 基座 MySQL 到本地只读身份快照链路；新增 `kb_identity_tenants`、`kb_identity_users`、`kb_identity_roles`、`kb_identity_user_roles`、`kb_identity_sync_runs` 初始化 schema，新增 `scripts/sync_ai_base_identity.py` 从 `system_tenant`、`sys_user`、`sys_role`、`sys_user_role` 只同步 BRD 允许的身份快照字段，手机号 / 邮箱写入前脱敏且不查询 `sys_user.password`；新增 `tests/test_ai_base_identity_sync.py`，补 `PyMySQL` 依赖。已连接用户提供的 AI 基座 MySQL `192.168.2.212:3306`，自动发现 schema `tellyes_ai`，同步 limit=5 的样本到本地 PostgreSQL：1 个租户、4 个用户、1 个角色、4 条用户角色关系；复核快照表计数和 `kb_identity_sync_runs` 最近记录为 success；验证 `pytest tests/test_ai_base_identity_sync.py tests/test_db_init.py -q` 7 passed。同步 `.planning/phases/11-01-identity-access-foundation/SUMMARY.md` 和 `docs/product/ai-base-sso-integration-guide.md` 当前快速贯通状态。
> 2026-06-18 / T-124 / DONE / codex / docs: 将 AI 基座租户端 SSO 对接方案输出为帮助文档 `docs/product/ai-base-sso-integration-guide.md`，覆盖一次性 `sso_code` 跳转、服务端 code 换身份摘要、身份快照增量同步、知识库侧处理规则、安全约束、错误码、日志审计和最小联调清单；同步更新 `docs/product/README.md` 与 `docs/document-map.md` 文档入口。
> 2026-06-18 / T-123 / DONE / codex / planning: 读取 `docs/product/knowledge-base-governance-brd.md` 第 21 章迭代方案，并按 GSD 主线拆分为 v7.0 知识库治理与开放能力计划。新增 `.planning/phases/11-01-identity-access-foundation/`、`11-02-kb-business-loop/`、`11-03-rag-graph-evaluation-governed/`、`11-04-openapi-api-key/`、`11-05-configuration-governance/`、`11-06-logs-monitoring-usage/` 的 `CONTEXT.md` 与 `PLAN.md`；同步 `.planning/ROADMAP.md`、`.planning/REQUIREMENTS.md`、`.planning/STATE.md`、`.planning/PROJECT.md` 与 `.planning/phases/README.md`，当前计划入口为 `.planning/phases/11-01-identity-access-foundation/PLAN.md`。
> 2026-06-18 / T-122 / DONE / codex / docs: 按用户确认收敛知识库治理 BRD 的第二批权限与配置边界。明确身份数据同步第一期采用“定时增量同步 + 登录时兜底刷新”，webhook / 事件推送仅作为后续高敏事件加速通道；创建人离职 / 禁用后历史知识库不自动删除，由租户管理员接管、转移当前业务归属人或删除，`created_by` 保留审计；第一期不做普通用户共享，仅支持个人创建个人可见、管理员全量可见；API Key 查询、文档上传、入库等能力按显式能力范围授权，写操作必须具备对应能力并强制签名、IP 白名单、nonce 防重放和审计；生产环境默认强签名 + IP 白名单 + nonce 防重放，其他系统不得传输真实 API Key 明文，只能使用 `api_key_id`、`app_id`、别名、配置引用或一次性代理 token；配置中心全局配置仅平台管理员可改，租户主数据只读同步，知识库配置可由当前业务归属人或管理员修改，API Key 配置仅管理员可改；已同步 `docs/product/knowledge-base-governance-brd.md` 与 `docs/product/README.md`。
> 2026-06-18 / T-121 / DONE / codex / docs: 按用户确认补充日志与敏感信息保留策略。明确日志只保留证据链，不保留可复现敏感内容；API Key、模型 Key、AK/SK、SSO code、session token、签名 secret、AI 基座 token、用户密码、密码 hash 和盐值永不明文入库、入日志或进入导出文件；OpenAPI、RAG / Graph RAG、入库任务、大模型调用、Prompt Profile 和错误日志默认只记录元数据、摘要、hash、fingerprint、引用 ID、风险标签和统计字段；完整 query、answer、prompt、provider 原始响应或文档正文仅允许管理员显式开启短期调试留痕，默认 7-14 天自动清理并全程审计；nonce 防重放记录建议仅保留 10-30 分钟，安全排查最长不超过 24 小时；日志导出文件建议 24 小时自动清理；已同步 `docs/product/knowledge-base-governance-brd.md` 与 `docs/product/README.md`。
> 2026-06-18 / T-120 / DONE / codex / docs: 按用户确认补充知识库管理员识别硬规则。明确第一期优先使用 AI 基座角色快照中的 `role_code=superManager` 判断租户管理员 / 知识库管理员，命中即具备本租户知识库管理员能力；未命中则认为是具备知识库自有归属权限的普通用户，只能管理本人作为当前业务归属人的知识库及其子资源；知识库不从菜单权限、按钮权限点或其他本地权限配置推断管理员身份；平台管理员角色编码独立待 AI 基座确认，不从 `superManager` 自动推断；已同步 `docs/product/knowledge-base-governance-brd.md` 与 `docs/product/README.md`。
> 2026-06-18 / T-119 / DONE / codex / docs: 按用户确认补充知识库历史数据初始化与迁移策略。明确首次上线前先同步 AI 基座租户、用户、角色、用户角色关系只读快照，快照为空或失败时仅平台管理员可进入初始化 / 诊断页面；历史知识库必须补齐 `tenant_id`，单租户数据补默认租户，已有映射则按创建人或业务归属人映射，无法映射则进入待认领 / 管理员待处理状态且普通用户不可见；历史 `owner_user_id` 优先使用 `created_by`，失效创建人对应知识库保留但进入失效归属人状态，由管理员接管、转交或删除；`created_by` 作为不可变审计字段不因迁移、接管或转交覆盖；历史测试 API Key 默认禁用或过期并由管理员重新颁发，不回写旧明文 key；历史日志和 token 统计能补归属则补，补不出则只读归档且仅管理员可查；迁移脚本必须可重复执行、可失败后重跑或回滚、输出迁移报告并写审计；已同步 `docs/product/knowledge-base-governance-brd.md` 与 `docs/product/README.md`。
> 2026-06-18 / T-118 / DONE / codex / docs: 按用户确认补充知识库配置中心生效方式。明确配置按热生效、灰度生效、重启或运维变更三类管理；第一期只承诺热生效和灰度生效，不承诺所有配置都能后台热改。Prompt Profile、API Key 策略、知识库问答参数、日志保留天数、脱敏策略和告警阈值热生效；模型选择、rerank provider、embedding provider / 模型、切片策略、清洗策略、质量过滤策略、Graph RAG 扩展参数和系统提示词大版本按租户、知识库或 API Key 灰度生效并可回滚；数据库连接、pgvector / Redis / OSS 配置、模型密钥、AK/SK、解析 provider 大切换、文件存储路径、队列 worker 数和后台任务并发全局上限走重启或管理员运维变更；已同步 `docs/product/knowledge-base-governance-brd.md` 与 `docs/product/README.md`。
> 2026-06-18 / T-117 / DONE / codex / docs: 按用户确认将 OpenAPI 文档、API Key 默认安全策略、API Key 绑定上限和日志导出规则固化到知识库治理 BRD。明确 OpenAPI 文档作为后台内置文档页，仅管理员可见、不匿名公开，展示接口、签名规则、错误码和示例但不展示真实 API Key；Bearer Token 仅建议用于内网或低风险联调，生产对外调用推荐强签名，高风险接口强制签名，IP 白名单第一期必须支持且生产建议配置；单个 API Key 默认最多绑定 20 个知识库，超过建议拆分 key；日志导出第一期不建设审批流，但必须限制权限范围、时间范围、最大条数、导出字段脱敏并写审计；已同步 `docs/product/knowledge-base-governance-brd.md` 与 `docs/product/README.md`，并从 BRD 待确认问题移除 OpenAPI 文档页面。
> 2026-06-18 / T-116 / DONE / codex / docs: 按用户确认补充知识库日志、审计与 Token 统计保留周期和可见范围。明确明细类日志默认保留 180 天，包含入库任务、API 调用、RAG / Graph RAG 查询、大模型调用明细、身份与访问判断、错误日志；审计类日志默认保留 365 天，包含操作审计、API Key 生命周期、配置变更、日志导出；聚合类统计默认保留 2 年，包含小时 / 日维度 token 聚合、调用量聚合、超阈次数和成本趋势；普通用户仅可查看本人归属知识库的业务日志摘要，租户管理员可看本租户全量日志，平台管理员可看全局日志；已同步 `docs/product/knowledge-base-governance-brd.md` 与 `docs/product/README.md`，并从 BRD 待确认问题移除日志保留周期。
> 2026-06-18 / T-115 / DONE / codex / docs: 按用户确认补充知识库额度与用量边界，明确第一期 QPS、每日调用量、总调用量、并发数和 token 预算只做统计、报表、趋势、超阈标记和告警，不作为强制拦截条件；超过统计阈值时 OpenAPI 问答、Graph RAG、文档读取等业务调用仍继续执行，`RATE_LIMITED` 和 `QUOTA_EXCEEDED` 仅作为后续启用强限制时的预留错误码；API Key 无效、禁用、过期、签名失败、IP 不允许、`kb_id` 缺失或未绑定、租户不可用等安全和结构性场景仍必须拒绝；已同步 `docs/product/knowledge-base-governance-brd.md` 与 `docs/product/README.md`。
> 2026-06-18 / T-114 / DONE / codex / docs: 按用户确认补充知识库删除策略，并增加“删除建议按照知识库创建的归属”边界。明确知识库、文档、入库任务默认软删除，删除权限统一按所属知识库 `owner_user_id`、租户管理员、平台管理员判断，不按文档上传人或任务创建人单独拆分；删除后子资源立即从后台、OpenAPI、RAG、Graph RAG、评测查询中不可见；日志、审计、API 调用和 token 统计保留脱敏记录；软删除保留期内可恢复，物理删除仅作为管理员高危操作或保留期后的后台清理任务；已同步 `docs/product/knowledge-base-governance-brd.md` 与 `docs/product/README.md`。
> 2026-06-18 / T-113 / DONE / codex / docs: 按用户确认补充知识库 OpenAPI 的租户状态校验策略。明确 API Key 调用不每次实时请求 AI 基座校验租户状态，默认使用知识库本地租户只读快照完成校验；快照 5 分钟内正常放行，5-10 分钟降级放行并记录 `tenant_snapshot_stale=true` 与告警，超过 10 分钟拒绝并返回 `TENANT_STATUS_STALE`，租户停用 / 冻结 / 删除 / 不明确时返回 `TENANT_DISABLED` 或 `TENANT_UNAVAILABLE`；已同步 `docs/product/knowledge-base-governance-brd.md` 与 `docs/product/README.md`，并从待确认问题移除该项。
> 2026-06-18 / T-112 / DONE / codex / docs: 按用户确认补充知识库 BRD 的 AI 基座身份快照同步失败 / 延迟降级策略。明确敏感新增、修改、删除、导出、配置变更、API Key 管理、接管 / 转交等操作默认拒绝；低风险读取可临时使用未超过窗口的旧快照，默认 10 分钟，可配置范围建议 5-15 分钟；旧快照命中必须记录 `identity_snapshot_stale=true`、快照版本、快照时间和降级原因，并在身份与访问监控中展示旧快照命中次数、最大快照年龄和告警；已从 BRD 待确认问题中移除旧快照窗口未决项，并同步 `docs/product/README.md`。
> 2026-06-18 / T-111 / DONE / codex / docs: 按用户确认补充知识库 BRD 的失效用户资源处置边界。明确 AI 基座账号禁用、离职或移出租户后，该用户不得继续登录知识库后台或操作知识库资源；其历史创建的知识库不自动删除，文档、入库、图谱、评测、日志和 token 统计继续归属原知识库与租户；租户管理员可接管、转交或删除本租户失效用户历史知识库，平台管理员可按全局范围处理；`created_by` 保留原创建人审计，不因接管或转交覆盖，新增 / 明确 `owner_user_id` 作为当前业务归属人，普通用户可见性与操作权限按当前归属人判断；同步 `docs/product/README.md` 入口说明。
> 2026-06-18 / T-110 / DONE / codex / docs: 收敛 `docs/product/knowledge-base-governance-brd.md` 的 AI 基座权限能力映射边界。明确知识库不消费菜单权限、按钮权限点、会话记录、学生、教职工、分组、标签等复杂组织画像；允许保存 AI 基座租户、用户、角色、用户角色关系只读快照，仅用于展示、审计、租户隔离和管理员身份判断；知识库访问规则改为同租户前提下“创建人可见可管本人知识库、租户管理员可管本租户全部、平台管理员可管全局”，其中普通用户可删除本人知识库及其文档、入库、图谱和评测数据，但配置中心和 API Key 管理仅管理员可用；日志可见性按管理员全量、普通用户仅本人知识库业务日志收敛；租户级 API Key 调用知识库资源必须指定 `kb_id`，未指定返回 `KB_ID_REQUIRED`，绑定多个知识库不代表单次跨库召回；系统提示词通过 Prompt Profile 或管理员级配置 API 受控覆盖，普通问答接口不得传 raw `system_prompt`；同步数据模型、前端页面、核心流程、验收标准、迭代规划、风险与术语，并更新 `docs/product/README.md` 入口说明。
> 2026-06-17 / T-109 / DONE / codex / docs: 新增并修订知识库治理与开放能力业务需求文档 `docs/product/knowledge-base-governance-brd.md`。文档结合 AI 基座租户端权限表，明确硬边界：知识库系统不维护任何权限、用户和角色，不新增、修改、删除权限 / 用户 / 角色相关数据，所有后台访问、知识库访问、文档访问、入库、问答、日志和配置操作均以 AI 基座租户端约束规则为准；知识库支持 AI 基座免登录 / SSO 进入后台，但不保存用户密码、密码 hash、盐值或登录凭证；知识库颁发的 API Key 仅用于消费知识库自身 OpenAPI，不得作为 AI 基座身份、服务账号或其他系统通用凭证；同时补充数据库设计总览、OpenAPI 与系统级凭证安全阶段，细化 API Key 强签名请求、签名版本、签名串、timestamp/nonce 防重放、body hash 防篡改、服务端保存、防泄露、防注入、系统级错误码、风险审计和告警要求；按身份与访问、知识库业务、文档入库、在线问答、图谱评测、配置治理、日志审计、开放接口、额度用量、监控报表拆分功能域，覆盖现有控制台页面，并拆成身份与访问底座、知识库业务闭环、问答图谱评测、OpenAPI/API Key、配置中心、日志监控用量与 Token 统计 6 个迭代；新增按链路管道归属的大模型 token 消耗统计、数据模型、验收和风险要求；同步更新 `docs/product/README.md` 与 `docs/document-map.md` 文档入口。
> 2026-06-17 / T-108 / DONE / codex / docs: 清理并合并陈旧文档归档。将 `docs/performance-discussions/` 正文统一移动到 `docs/archive/performance-discussions/`，旧目录仅保留跳转 README；将历史计划、历史架构分析、Claude/Codex 迁移参考和早期链路旧入口收拢到 `docs/archive/` 对应子目录；同步更新 `docs/document-map.md`、`docs/README.md`、`docs/archive/README.md`、`docs/agent/README.md`、`docs/architecture/README.md` 与性能档案关联链接，保持当前入口指向 `docs/pipeline/`、`docs/product/` 和 `docs/performance-optimizations/`。
> 2026-06-17 / T-107 / DONE / codex / docs: 按当前项目逻辑整理文档归类与完整链路说明。新增 `docs/document-map.md` 作为文档归类地图，新增 `docs/product/README.md`、`docs/archive/README.md`、`docs/archive/legacy-chain/README.md`；将产品招标参数正文归档到 `docs/product/technical-bid-parameters.md`，将根目录完整链路正文归档到 `docs/pipeline/full-chain-technical-guide.md`，旧入口保留迁移提示。重写完整链路文档，按离线入库、普通 RAG、Graph RAG、图谱预览、控制台、配置、指标和后续需求挂点细化到关键模块、数据表、输入输出与边界；同步更新根 `README.md`、`docs/README.md`、`docs/pipeline/README.md`。
> 2026-06-17 / T-106 / DONE / codex / feature: Added and archived official MinerU parser channel `PDF_PARSER_PROVIDER=mineru_official` with independent `MINERU_OFFICIAL_API_TOKEN`. Official-only sharding remains scoped to `core/parser/mineru_official_parser.py` and `MINERU_OFFICIAL_SHARDING_*`; existing 302AI `mineru` (`parser.cloud.sharding.*`) and `ali_document_mind` (`ALIYUN_DOCUMENT_MIND_SHARDING_*` plus key-pool scheduling) are unchanged. Verified fast profile is `MINERU_OFFICIAL_MODEL_VERSION=pipeline` + `20` pages/shard + `6` workers + `30MB` max/shard; same 396-page / 44.9MB textbook parse improved from `497166ms` (`vlm + 180 pages/shard + 2 workers`) to `121773ms`, about `4.08x` faster. Added env templates, console settings exposure, tests, and archived details in `docs/pipeline/mineru-official-parser.md` plus `.planning/quick/20260617-mineru-official-provider/SUMMARY.md`.
> 2026-06-17 / T-105 / DONE / codex / docs: 阿里 Document Mind 解析性能优化结档。当前封存档为 `33页/片 + 4 worker + 单 Key 并发 1 + probe1 + No-LLM + markdown,visualLayoutInfo + 4 组已验证 AK/SK`；确认当前稳定档不需要 6 组 AK/SK，冗余 key 可移除。已补 `docs/performance-optimizations/parse/document-mind-optimization-process.md` 结档状态、`docs/performance-optimizations/parse/README.md` 结档说明、`docs/performance-optimizations/README.md` 当前基线与 `.planning/STATE.md` 主线状态。后续不再围绕同一 provider 做常规调参，仅在 provider 行为变化、新文档类型、可复现回退、明确证据取舍或 provider 级 A/B 时重启。

> 2026-06-16 / T-104 / DONE / codex / perf: 修复 Document Mind 最新解析变差问题。确认 `ALIYUN_DOCUMENT_MIND_LLM_ENHANCEMENT=false` 仍然关闭，回退根因不是 LLM 增强，而是运行中的 Docker backend 仍使用 6-key pool；4 worker 稳态下继续探测/使用未知慢 key，使 `dm-key-5` 这类慢 key 进入解析路径。已将 `_DocumentMindCredentialPool` 调度改为按 `parseKeyActiveTarget` 收敛：冷启动允许补齐实际 worker 目标数，目标满足后不再继续探未知 key；指标新增 `parseKeyActiveTarget`，并保留匿名 `avgMs/lastMs` 历史。`.env` 已将 Document Mind credential pool 收敛到当前 4 个已验证 key，backend 已 `docker compose up -d --build backend` 重建。真实 parse-only 复核 `data/results/document_mind_scheduler_4key_benchmark.jsonl` 有效记录：`parseWallMs=84042`、`parseKeyPoolSize=4`、`parseKeyActiveTarget=4`、`parseKeyThrottleCount/retry/cooldown=0`、`parseProviderManagedLlmEnabled=0`、`outputBlocks=18500`、`outputImageBlocks=475`、`outputTableBlocks=81`。验证通过：`python -m py_compile core/parser/document_mind_parser.py`、`pytest tests/test_document_mind_parser.py -q`、`pytest tests/test_pdf_sharding.py tests/test_document_mind_parser.py tests/test_runtime_settings.py tests/test_api_console.py -q`。

> 2026-06-16 / T-103 / DONE / codex / perf: 按用户要求直接继续跑 Document Mind 解析优化验证，不再停留在建议层。Docker daemon 当前不可用，已在本地 Python 环境安装 `alibabacloud-docmind-api20220711` 后跑真实 Document Mind canary 与整本 parse-only 复核；canary 固定为 4 个 33 页 shard，比较 `ALIYUN_DOCUMENT_MIND_POLL_INTERVAL=1/2/3`，结果受 provider 队列波动影响明显：`poll1=285671ms`、`poll2=241954ms`、`poll3=34765ms`，输出均为 `5371 blocks / 138 image / 35 table`，因此不下调默认 poll 间隔。随后用当前稳定档整本复核 `data/uploads/080ed361-f4f5-4be6-b134-99d53ecbd19a.pdf`：`parseWallMs=135405`、`shardCount=12`、`parseWorkerCount=4`、`outputBlocks=18500`、`outputImageBlocks=475`、`outputTableBlocks=81`、`parseKeyThrottleCount/retry/cooldown=0`、`resultFetchSkippedByStatus=0`（未命中 T-102 短路）。当前最优结论保持 `33页/片 + 4 worker + 单 Key 并发 1 + No-LLM + markdown,visualLayoutInfo + poll=3 + shard save garbage=1`。
> 2026-06-16 / T-102 / DONE / codex / perf: 继续针对 Document Mind 解析 provider 热路径做小步优化：当 `QueryDocParserStatus` 的 success 响应已经携带 markdown / 结构化主内容时，直接转换该状态响应并跳过当前 job 的额外 `GetDocParserResult` 往返；若状态响应只有页面图片/layout 证据或无法转换出主内容，仍保持旧逻辑拉取 result 并合并图片证据。新增 `resultFetchSkippedByStatus`、`statusConvertMs/statusConvertMsMax` 指标与回归测试；该优化只复用同一次轮询得到的响应，不跨任务缓存解析结果，也不复用历史 `ContentBlock[]` 或 shard 结果。验证通过：`python -m py_compile core/parser/document_mind_parser.py` 与 `pytest tests/test_document_mind_parser.py -q`。
> 2026-06-16 / T-101 / DONE / codex / perf: 继续优化 Document Mind 解析性能，在不引入解析结果缓存、不复用历史 `ContentBlock[]` 或 shard 结果的前提下，改为仅在 Document Mind 分片路径使用轻量 PDF shard 保存策略。新增 `PdfShardSaveOptions`，默认 Document Mind 使用 `garbage=1, deflate=true`，MinerU/通用分片默认仍保持原 `garbage=4, deflate=true`；补充 `ALIYUN_DOCUMENT_MIND_SHARD_SAVE_GARBAGE`、`ALIYUN_DOCUMENT_MIND_SHARD_SAVE_DEFLATE` 运行时设置、控制台入口、env 模板、`parseShardSaveGarbage/parseShardSaveDeflate` 指标、测试与文档。基于 `data/uploads/080ed361-f4f5-4be6-b134-99d53ecbd19a.pdf` 的本地 split 微基准显示 12 个 shard 从约 `782ms` 降到约 `282ms`，总 shard 字节仅小幅增加；已通过 `py_compile` 与 `pytest tests/test_pdf_sharding.py tests/test_document_mind_parser.py tests/test_runtime_settings.py tests/test_api_console.py -q`。
> 2026-06-15 / T-100 / DONE / codex / perf: 继续推进 Document Mind 解析方案验证，新增“混合解析 dry-run 探针”而不改线上默认行为。`scripts/plan_document_mind_shards.py` 现在输出 `hybridParse` 画像，基于 PyMuPDF 逐页轻量特征把页面分成本地文本候选 / 云端或不确定候选；新增 `tests/test_document_mind_shard_plan.py` 覆盖保守分类逻辑。验证结果：228MB / 494 页扫描教材本地文本候选 `0/494`，47MB / 396 页常用教材候选仅 `21/396`（约 `5.3%`），因此对当前代表样本混合解析不能显著缩短云端解析墙钟，后续优先级低于并行流水线与 provider 级复验。
> 2026-06-15 / T-099 / DONE / codex / perf: 使用 GSD quick 按用户约束推进“无解析结果缓存”的 Document Mind 解析性能优化。已明确排除跨任务复用解析结果、复用历史 `ContentBlock[]` 或复用历史 shard 结果；本轮已实现复杂度感知分片 + 重片优先调度：`inspect_pdf(profile_pages=True)` 提取 per-page 文本/图像/扫描页轻量特征，`plan_weighted_page_ranges()` 保持 shard 数量并尽量压低最重 shard，最终仍按全局页码合并。新增 `parseWeightedShardingEnabled`、`parseShardWeight*`、`parseHeaviestShard*`、`parseHeavyShardFirstEnabled` 指标，并补 runtime setting、控制台热更新入口、env 模板、benchmark 候选、测试与性能文档。验证通过：`py_compile`、`pytest tests/test_pdf_sharding.py tests/test_document_mind_parser.py tests/test_runtime_settings.py tests/test_api_console.py -q`；真实同教材 A/B 显示 fixed `100560ms` 优于 weighted-heavy-first `136111ms`，输出同为 `18500 blocks / 475 image / 81 table`，因此 weighted/heavy-first 默认关闭，仅作为显式实验档保留。2026-06-15 已并入主线快验证漏斗：新增 `scripts/plan_document_mind_shards.py` 本地 dry-run 和 `benchmark_document_mind_parse.ps1 -PageRanges` canary 入口；后续先用 dry-run/canary 收敛，只有出现正信号才跑完整云端 A/B。

> 2026-06-15 / T-098 / DONE / codex / perf: 使用 GSD quick 增加 `INGESTION_READY_MODE=basic|basic_ready` 基础可检索入库模式；默认保持 full 完整增强链路不变，开启后 hierarchical 切片传入 `enable_enhanced=False`，先跳过 enhanced 层，让知识库以基础 child/table/image chunks 完成质量门控、向量化和写库。已补 runtime setting、控制台设置入口、env 模板、`chunkTimings.readyMode/requestedReadyMode/readyModeSource/enhancementSkipped` 留痕、任务 payload `ingestionReadyMode`、GSD quick 文档与复盘文档；`pytest tests/test_chunker.py tests/test_ingestion_embedding_metrics.py tests/test_api_console.py tests/test_runtime_settings.py tests/test_backend_app.py -q` 通过，`74 passed`。本切片尚未实现后台增强队列，下一步需同教材 A/B 验证 full vs basic 的基础可检索耗时和召回质量。

> 2026-06-15 / T-097 / DONE / codex / perf: 使用 GSD quick 继续验证 `PGVECTOR_WRITE_MODE=copy` 后的 pgvector 写入路径，新增 `scripts/benchmark_pgvector_write_path.py`，复用成功 KB `bench-p33-c4-no-llm-layout-full-20260615-082020-da45` 的真实 `2985` chunks / `7037` relations / `41.8MB` chunk CSV payload，重映射 UUID 并在事务末尾 `ROLLBACK`，不留下 benchmark 数据。远程 DB 验证显示 COPY 到临时表约 `3976-8216ms`、typed 临时表 insert 约 `900-994ms`、indexed chunks 主表 insert `2495-3577ms`、relations insert 低于 `0.5s`；本地临时 pgvector 将 COPY 传输压到 `2032-2736ms`，但 indexed chunks insert 仍为 `5341-6162ms`。结论：relations/triples 与 vector cast 不是剩余主因，DB locality 只改善传输侧；下一步要从索引维护策略、bulk load 取舍或 basic-ready + async write 模式继续降耗。
> 2026-06-15 / T-096 / DONE / codex / perf: 使用 GSD quick 验证 Document Mind No-LLM 快速解析档的完整入库效果，并继续压缩 pgvector 写库。新增 `scripts/benchmark_document_mind_ingestion.ps1`，修复轮询超时误判；`PGVECTOR_WRITE_MODE=copy` 通过临时表 + `COPY FROM STDIN` 写入 `chunks/chunk_relations/kg_triples`，export stage 增加 `pgvectorChunksWriteMs/RelationsWriteMs/TriplesWriteMs` 等拆账。真实同教材完整入库验证：最佳 warm run `taskWallMs=212885ms`，`parse=84375ms`、`chunk=74471ms`、`embedding=16336ms`、`export=22710ms`；COPY 复核成功任务 `5bfebd2d-7a8d-49b9-8de0-5765cde90b82` 为 `parse=124088ms`、`chunk=71933ms`、`embedding=16527ms`、`export=16578ms`，其中 `pgvectorChunksWriteMs=15904ms`、relations 仅 `327ms`。结论：关闭托管 LLM/VLM 已通过完整入库验证，COPY 将 export 降到约 `16.6s`，剩余主瓶颈为 Document Mind parse 与三层增强 chunk；export 内部瓶颈已转为远程 DB 上的 chunks 主表向量写入/索引维护。
> 2026-06-14 / T-095 / DONE / codex / perf: 使用 GSD quick 继续压缩 Document Mind 解析耗时，确认 `hedged shard` 投机重发无收益并保持默认关闭；有效提速方向改为关闭 Document Mind 托管 LLM/VLM 增强，同时保留 `ALIYUN_DOCUMENT_MIND_OUTPUT_FORMAT=markdown,visualLayoutInfo` 以维持图片/layout 证据。真实同教材 parse-only 验证：`p33-c4-no-llm-layout=102131ms`，repeat `97082ms`；same-window managed baseline `p33-c4-probe1=381948ms`，早前 baseline `188911ms`。当前快速解析档已热更新为 `LLM_ENHANCEMENT=false`、`HEDGED_SHARD_ENABLED=false`、`PAGES_PER_SHARD=33`、`SHARDING_MAX_CONCURRENCY=4`、`MAX_INFLIGHT_PER_KEY=1`、`KEY_PROBE_CONCURRENCY=1`、`TARGET_WAVES=2`。风险：关闭托管增强后 parse blocks 从约 `8.3k` 增至 `18.5k`，下一步必须用完整入库验证 clean/chunk 是否吞得住。
> 2026-06-14 / T-094 / DONE / codex / perf: 使用 GSD quick 为 Document Mind 解析凭证池增加慢 key 评分与未知 key 探测上限。解析调度现在记录匿名 `dm-key-N` 成功耗时 `lastMs/avgMs`，使用进程级历史降权慢 key，并新增可热更新 `ALIYUN_DOCUMENT_MIND_KEY_PROBE_CONCURRENCY`；真实同教材 parse-only 验证显示 `p33-c4-probe1=192911ms`、最终稳态 `p33-c4/probe1=208043ms`，明显优于 `probe2=362534ms`、`probe3=346032ms`，全程 throttle/retry/cooldown 为 0。当前最优稳态为 `PAGES_PER_SHARD=33`、`SHARDING_MAX_CONCURRENCY=4`、`MAX_INFLIGHT_PER_KEY=1`、`KEY_PROBE_CONCURRENCY=1`、`TARGET_WAVES=2`。未调整清洗、切片、向量化、入库或检索逻辑。
> 2026-06-14 / T-093 / DONE / codex / observability: 为项目内直接调用 LLM / Embedding 的环节补齐 token usage 统计，并将 Document Mind 解析服务流量与本地模型 token 拆账。新增统一 `core/llm_usage.py`，清洗、质量门控、普通 LLM 切片、三层增强、embedding、在线 RAG 生成/评分/LLM 检查均从 OpenAI 兼容响应 `usage` 聚合 prompt/completion/total tokens；入库阶段 metrics 暴露 `llmCleaner*`、`qualityLlm*`、`llmChunker*`、`enhance*Tokens`、`embedding*Tokens` 等字段。Document Mind parse metrics 另增 `parseServiceRequests/InputPages/InputBytes/ShardBytes` 与 `parseProviderManagedLlmEnabled`，明确 provider 内置 LLM 增强 token 不由本项目直接可见。`python -m py_compile ...` 与 `pytest tests/test_embedding_client.py tests/test_rag_adapter.py tests/test_document_mind_parser.py tests/test_chunker.py tests/test_ingestion_embedding_metrics.py tests/test_generator.py tests/test_reranker.py tests/test_scorer.py -q` 通过，85 passed。
> 2026-06-13 / T-092 / DONE / codex / perf: 使用服务端热更新 + 容器内 parse-only 自动验证脚本，基于同一本常用教材 PDF 对 Document Mind 解析档位做了 33/30/36 页与 4/5 worker 的对照。最终确认当前最优仍是 `PDF_PARSER_PROVIDER=ali_document_mind`、`ALIYUN_DOCUMENT_MIND_SHARDING_PAGES_PER_SHARD=33`、`ALIYUN_DOCUMENT_MIND_SHARDING_MAX_CONCURRENCY=4`、`ALIYUN_DOCUMENT_MIND_MAX_INFLIGHT_PER_KEY=1`：本轮 `p33-c4=166860ms`，`p30-c4=184736ms`，`p36-c4=359430ms`，`p33-c5=437000ms`，全部无 throttle/retry/cooldown。已回写热更新到服务端稳态档，并新增 `scripts/benchmark_document_mind_parse.ps1` 供后续继续自动 sweep。未调整清洗、切片、向量化、入库或检索逻辑。
> 2026-06-13 / T-091 / DONE / codex / perf: 针对 6 组 Document Mind AK/SK 反而解析变慢的复盘，已将解析凭证池与 shard 并发上限解耦。`ALIYUN_DOCUMENT_MIND_CREDENTIAL_POOL` 可继续保留 6 组用于轮换和观测，但未显式配置 `ALIYUN_DOCUMENT_MIND_SHARDING_MAX_CONCURRENCY` 时不再按凭证组数自动派生并发；代码、控制台默认值、`.env` 与 env 模板均回到当前稳定档 `PAGES_PER_SHARD=33`、`SHARDING_MAX_CONCURRENCY=4`、`MAX_INFLIGHT_PER_KEY=1`、`TARGET_WAVES=2`。未调整清洗、切片、向量化、入库或检索逻辑。
> 2026-06-12 / T-090 / DONE / codex / perf: 4 组 AK/SK 的 Document Mind 解析档已收敛。最新同教材复盘任务 `080ed361-f4f5-4be6-b134-99d53ecbd19a` 在 `PDF_PARSER_PROVIDER=ali_document_mind`、`ALIYUN_DOCUMENT_MIND_SHARDING_PAGES_PER_SHARD=33`、`ALIYUN_DOCUMENT_MIND_SHARDING_MAX_CONCURRENCY=4`、`ALIYUN_DOCUMENT_MIND_MAX_INFLIGHT_PER_KEY=1` 下跑到 `parseWallMs=147212`，`shardCount=12`、`parseWorkerCount=4`、`parseKeyThrottleCount/retry/cooldown=0`；不动清洗、切片、向量化、入库后逻辑。
> 2026-06-12 / T-089 / DONE / codex / perf: 继续优化 Document Mind 解析瓶颈。范围限定在解析 provider、分片粒度和 parse metrics：已补 `inspect/split/submit/poll/resultFetch/convert/merge/shardWall` 耗时拆账，并让 `PAGES_PER_SHARD=40` 作为上限，在多 AK/SK 容量足够时自适应收敛到更小 shard 以降低 provider 长尾；新增 `ALIYUN_DOCUMENT_MIND_SHARDING_MIN_PAGES_PER_SHARD` 与 `ALIYUN_DOCUMENT_MIND_SHARDING_TARGET_WAVES`。未调整 UI、清洗、切片、向量化或入库后逻辑；`python -m py_compile ...` 与 `pytest tests/test_document_mind_parser.py tests/test_parser_provider.py tests/test_backend_app.py tests/test_api_console.py tests/test_runtime_settings.py -q` 通过，74 passed。
> 2026-06-12 / T-088 / DONE / codex / observability: 补齐 Document Mind 多 AK/SK 解析凭证池在控制台的可见性。新增 `ParseKeyMetricsPanel`，在全局入库页和单知识库入库工作台展示 parse stage 已有 `parseKey.*` 指标，包括凭证池组数、单 Key 并发、限流/重试/冷却、匿名 `dm-key-N` 调用/成功/失败/限流/总耗时分布；仅消费已有 metrics，不改变解析、清洗、切片、向量化、入库或检索逻辑。`frontend npm run typecheck` 通过。
> 2026-06-11 / T-087 / DONE / codex / perf: 使用 GSD 计划并执行 10-10 Document Mind 多 AK/SK 解析并发。已新增 `ALIYUN_DOCUMENT_MIND_CREDENTIAL_POOL`、`ALIYUN_DOCUMENT_MIND_MAX_INFLIGHT_PER_KEY`、`ALIYUN_DOCUMENT_MIND_KEY_RETRIES`、`ALIYUN_DOCUMENT_MIND_KEY_COOLDOWN_SECONDS`；`ali_document_mind` 分片解析现在按 `min(shardCount, shardingMaxConcurrency, poolSize * maxInflightPerKey)` 计算实际并发，同一 shard 的 submit/poll/result 使用同一匿名 `dm-key-N`，限流才触发 cooldown 与换 key 重试；parse stage metrics 增加 `parseKey.*` 匿名 per-key 指标。已同步控制台 settings、env 模板、解析性能文档、离线入库文档与 `.planning/phases/10-10-document-mind-credential-pool/`；`python -m py_compile ...` 与 `pytest tests/test_document_mind_parser.py tests/test_parser_provider.py tests/test_backend_app.py tests/test_api_console.py tests/test_runtime_settings.py -q` 通过，73 passed。
> 2026-06-11 / T-086 / DONE / codex / perf: 真实验证 `3a3c1fd0-a0a8-4fea-87eb-2f1c98de3f15`，已将切片与向量化 key pool 都切到 8 组，`HIERARCHICAL_ENHANCE_MAX_CONCURRENCY` 提升到 `100`，并确认 `enhancePeakConcurrency=100`、`enhanceWallMs=76975`、`enhanceFailures=0`、`enhanceKeyThrottleCount=0`、`enhanceKeyRetryCount=0`、`enhanceLlmKeyPoolSize=8`、`embeddingKeyPoolSize=8`。key pool 解析上限已按代码支持到 20 组。`python -m py_compile backend/services/ingestion_service.py core/chunker/hierarchical.py core/embedding/client.py` 与相关 pytest 已通过，backend 也已重建并健康。
> 2026-06-11 / T-085 / DONE / codex / perf: 基于真实任务 `962ab89d-d5dd-47e6-bd15-5a3b504d7eb8` 的向量化拆账继续优化。该任务 `embedding.latencyMs=22507`、`embeddingWallMs=22132`、`linkSemanticMs=333`、`linkProcedureMs=15`、`linkCausalMs=16`、`pgvectorWriteMs=304`、`retryCount=0`、`embeddingKeyThrottleCount=0`，确认当前瓶颈已回到 embedding API 外呼墙钟；已将 `LLM_EMBEDDING_MAX_CONCURRENCY` 从 `8` 小步提升到 `10`，并把 `LLM_EMBEDDING_BATCH_SIZE` / `LLM_EMBEDDING_MAX_CONCURRENCY` / `LLM_EMBEDDING_MAX_RETRIES` 纳入控制台运行时设置。`python -m py_compile core/runtime_settings.py backend/services/console_service.py core/embedding/client.py` 与 `pytest tests/test_api_console.py tests/test_runtime_settings.py tests/test_embedding_client.py -q` 通过；已重建并重启 backend/frontend，`/api/health` 正常，`PUT /api/console/settings` 可热更新新 key。

> 2026-06-11 / T-084 / DONE / codex / perf: 使用 GSD 执行 10-07 在线 RAG 召回性能优化。已新增 `core/rag/retrieval_snapshot.py`、snapshot CTE 单次候选快照查询、`chunks.search_text/search_vector`、enhanced/related 内存处理、`RAG_RETRIEVAL_SNAPSHOT` 回退开关，并归档到 `docs/performance-optimizations/retrieval/README.md`；聚焦测试通过。


| ID | 状态 | Owner | 类型 | 范围 | 任务 | 依赖 | 更新时间 | 备注 |
|----|------|-------|------|------|------|------|-----------|------|
| T-083 | DONE | codex | perf | `core/chunker/semantic_linker.py` `tests/test_semantic_linker.py` `requirements.txt` `docs/performance-optimizations/embedding/README.md` | 优化向量化 finalize 中 `link_semantic()` 平方级后处理热路径 | T-080,T-077,T-082 | 2026-06-11 | 基于任务 `0bc0372e-27cb-4c0c-8ba3-fdc9394f6e01`：`embedding.latencyMs=275166` 但 `embeddingWallMs=23728`、`linkSemanticMs=251423`，确认多 key 已生效且瓶颈在 semantic linker。已新增 numpy 分块矩阵相似度路径，并将 `numpy` 声明为直接依赖；未安装 numpy 或关闭 `LINKER_SEMANTIC_NUMPY_ENABLED` 时回退到预计算范数、每 pair 只算一次的纯 Python 路径；保持 threshold/topk/skip_same_parent 和双向关系语义。合成 benchmark `600 x 256` 从旧式 Python cosine `12346ms` 降到 numpy 分块 `164ms`；`python -m py_compile core/chunker/semantic_linker.py`、`pytest tests/test_semantic_linker.py tests/test_relation.py tests/test_ingestion_embedding_metrics.py -q` 通过 |
| T-082 | DONE | codex | docs | `docs/performance-optimizations/` `docs/performance-discussions/README.md` `docs/pipeline/README.md` `docs/pipeline/three-layer-chunking-final-solution.md` `docs/README.md` `README.md` | 将性能优化文档从链路技术方案中隔离，并按解析、切片、向量化三线归档 | T-078,T-080,T-079,T-077,T-072,T-065 | 2026-06-11 | 新增性能优化档案总入口和 `parse/`、`chunk/`、`embedding/` 三条线文档；每条线补优化过程、技术演进、关键指标、当前结论和回退规则；`docs/pipeline/` 改为只承接稳定链路与最终方案，旧 `docs/performance-discussions/` 保留为历史材料 |
| T-081 | DONE | codex | bugfix | `core/rag/generator.py` `tests/test_generator.py` | 使用 GSD quick 最小化补全在线 RAG 忠实度引用兜底 | T-046,T-057 | 2026-06-11 | 仅在生成答案未输出 `[n]` 引用、但已有高置信召回上下文时，复用现有 extractive fallback 生成带引用答案；不改三层切片、关系边、召回结构或评分公式。回归覆盖 LLM 漏写引用时 `citations` 不为空；`pytest tests/test_generator.py tests/test_backend_app.py -q`、`pytest tests/test_scorer.py tests/test_rag_adapter.py tests/test_api_console.py -q` 通过 |
| T-080 | DONE | codex | bugfix | `core/chunker/relation_utils.py` `tests/test_relation.py` `docs/performance-discussions/embedding-performance.md` | 修复 finalize 语义链接中 `Relation.weight` 浮点微越界导致入库失败 | T-077,T-057 | 2026-06-11 | 基于任务 `7f07b802-ab7c-4dee-ab15-32fa946b613f`：解析和切片已完成，`3077` 切片、增强墙钟 `112575ms`，但 embedding 后处理 `link_semantic()` 生成 `1.0000000000000002` 权重触发 `Relation.weight <= 1` 校验失败。已在统一关系写入入口仅 clamp `1±1e-9` / `0±1e-9` 浮点误差，明显非法权重仍报错；`python -m py_compile core/chunker/relation_utils.py core/chunker/semantic_linker.py`、`pytest tests/test_relation.py tests/test_semantic_linker.py tests/test_ingestion_embedding_metrics.py -q` 通过 |
| T-079 | DONE | codex | bugfix | `core/parser/document_mind_parser.py` `core/runtime_settings.py` `backend/services/console_service.py` `.env` `.env.example` `.env.docker.example` `docs/performance-discussions/parse-performance.md` | 修复 Document Mind 分片解析 success 后空结果导致整条入库失败 | T-055,T-065 | 2026-06-11 | 基于任务 `d8339f18-3115-4507-96af-5bd953708e46`：`shard #4 P121-160` 返回 success 但无 markdown/layout/page records。已将 Document Mind 输出格式基线改为 `markdown,visualLayoutInfo`，新增同 job 结果重拉与当前 shard 重新提交保护，补 payload 摘要日志、控制台热更新参数和解析性能文档；`python -m py_compile core/parser/document_mind_parser.py core/runtime_settings.py backend/services/console_service.py`、`pytest tests/test_document_mind_parser.py tests/test_api_console.py tests/test_runtime_settings.py -q` 通过 |
| T-078 | DONE | codex | docs | `docs/performance-discussions/three-layer-chunking-optimization-archive.md` `docs/pipeline/three-layer-chunking-final-solution.md` `docs/performance-discussions/README.md` `docs/pipeline/README.md` | 将三层切片优化过程与最终技术方案分开归档 | T-076,T-072,T-066 | 2026-06-11 | 已拆成“优化过程归档”和“最终技术方案”两份文档；最终方案明确下载/资产准备属于解析边界，三层切片消费合并后的 `ContentBlock[]`，图片增强仅在本地 `image_path` 可读且 VL 可用时读图，否则文本 fallback；同时沉淀 `70` 当前最佳已验证档、key 池、热更新、观测指标和回退规则 |
| T-077 | DONE | codex | feature | `core/embedding/client.py` `backend/services/ingestion_service.py` `core/runtime_settings.py` `backend/services/console_service.py` `docs/performance-discussions/embedding-key-pool.md` `.planning/quick/260611-embedding-key-pool/` | 使用 GSD quick 为 embedding 增加 key 池复用方案与后处理拆账指标 | T-057,T-069,T-070,T-076 | 2026-06-11 | 新增 `LLM_EMBEDDING_API_KEY_POOL`，为空时复用 `LLM_API_KEY_POOL`，但 embedding 与切片增强的 inflight/cooldown/retry/metrics 独立；新增 `LLM_EMBEDDING_KEY_RETRIES`、`LLM_EMBEDDING_KEY_COOLDOWN_SECONDS`、匿名 per-key metrics、`linkSemanticMs/linkProcedureMs/linkCausalMs`、`entityMaterializeMs/pgvectorWriteMs`；不改变 embedding model、维度、chunk 顺序、schema 或召回策略；`python -m py_compile core/embedding/client.py core/runtime_settings.py backend/services/console_service.py backend/services/ingestion_service.py`、`pytest tests/test_embedding_client.py -q`、`pytest tests/test_ingestion_embedding_metrics.py tests/test_api_console.py tests/test_runtime_settings.py -q` 通过 |
| T-076 | DONE | codex | ops | `.env` `.env.example` `.env.docker.example` `docs/performance-discussions/` `docs/pipeline/offline-ingestion-pipeline.md` `docs/rule/hierarchical-chunking.md` `.planning/quick/260611-enhance-max-concurrency-70/` | 基于 `60` 档真实 0 限流结果，将三层切片增强全局并发热更新到 `70` 验证档 | T-075,T-074,T-073,T-071,T-070,T-069 | 2026-06-11 | 真实任务 `7571b640-6d0b-4bdb-8f83-f90b6902fb9f` 显示 `enhanceMaxConcurrency=70`、`enhancePeakConcurrency=70`、`enhanceWallMs=69365`、`enhanceFailures=0`、`enhanceKeyThrottleCount=0`、`enhanceKeyRetryCount=0`，5 个匿名 key 调用约 `187/188/176/186/195`；`70` 已成为当前最佳已验证切片档；同任务显示 `embedding.latencyMs=104651` 但 `embeddingWallMs=15285`、`retryCount=0`，下一步优先拆解向量化 stage 内部 semantic/procedure/causal 后处理 |
| T-075 | DONE | codex | ops | `.env` `.env.example` `.env.docker.example` `docs/performance-discussions/` `docs/pipeline/offline-ingestion-pipeline.md` `docs/rule/hierarchical-chunking.md` `.planning/quick/260611-enhance-max-concurrency-60/` | 基于 `50` 档真实 0 限流结果，将三层切片增强全局并发热更新到 `60` 验证档 | T-074,T-073,T-071,T-070,T-069 | 2026-06-11 | 真实任务 `0317ee49-b378-47f9-b1e5-e733a6661388` 显示 `enhanceMaxConcurrency=50`、`enhancePeakConcurrency=50`、`enhanceWallMs=95695`、`enhanceFailures=0`、`enhanceKeyThrottleCount=0`、`enhanceKeyRetryCount=0`，5 个匿名 key 调用约 `188/180/184/190/190`；已通过控制台 settings DB override 热更新到 `60` 并同步基线与优化追踪文档；本轮仍只调整 enhanced 外呼 hard cap，不改变 prompt、模型参数、触发条件、slot 合并顺序或最终 chunk；异常时优先回退 `50`，直接跳 `70` 暂不采用 |
| T-074 | DONE | codex | ops | `.env` `.env.example` `.env.docker.example` `docs/performance-discussions/` `docs/pipeline/offline-ingestion-pipeline.md` `docs/rule/hierarchical-chunking.md` `.planning/quick/260611-enhance-max-concurrency-50/` | 基于 `42` 档真实 0 限流结果，将三层切片增强全局并发热更新到 `50` 验证档 | T-073,T-071,T-070,T-069 | 2026-06-11 | 真实任务 `617c207a-0d9c-4e46-8736-a5f0a7050aad` 显示 `enhanceMaxConcurrency=42`、`enhancePeakConcurrency=42`、`enhanceWallMs=120164`、`enhanceFailures=0`、`enhanceKeyThrottleCount=0`、`enhanceKeyRetryCount=0`，5 个匿名 key 调用约 `184/186/180/190/192`；已通过控制台 settings DB override 热更新到 `50` 并同步基线与优化追踪文档；本轮仍只调整 enhanced 外呼 hard cap，不改变 prompt、模型参数、触发条件、slot 合并顺序或最终 chunk；异常时优先回退 `42` |
| T-073 | DONE | codex | ops | `.env` `.env.example` `.env.docker.example` `docs/performance-discussions/` `docs/pipeline/offline-ingestion-pipeline.md` `docs/rule/hierarchical-chunking.md` `.planning/quick/260611-enhance-max-concurrency-42/` | 基于 `34` 档真实 0 限流结果，将三层切片增强全局并发热更新到 `42` 验证档 | T-071,T-070,T-069 | 2026-06-11 | 真实任务 `bc79f5ef-4673-4bc5-943a-225b316bc463` 显示 `enhanceMaxConcurrency=34`、`enhancePeakConcurrency=34`、`enhanceWallMs=143985`、`enhanceFailures=0`、`enhanceKeyThrottleCount=0`、`enhanceKeyRetryCount=0`，5 个匿名 key 调用分布均衡；已通过控制台 settings DB override 热更新到 `42` 并同步基线文档；本轮仍只调整 enhanced 外呼 hard cap，不改变 prompt、模型参数、触发条件、slot 合并顺序或最终 chunk；异常时优先回退 `34` |
| T-072 | DONE | codex | docs | `docs/performance-discussions/chunk-enhancement-optimization-tracker.md` `docs/performance-discussions/README.md` | 整理三层切片增强优化过程、真实任务结果、观测演进和回退规则，并纳入此前尝试 | T-071,T-070,T-069,T-066,T-063,T-058,T-056 | 2026-06-11 | 已汇总原始 `7774156ms` 瓶颈、早期有序并发、动态借用、22/26/34/42/50/60/70 并发、多 key/per-key 观测与回退规则；当前 tracker 已将 `70` 标为最佳已验证档 |
| T-071 | DONE | codex | ops | `.env` `.env.example` `.env.docker.example` `docs/performance-discussions/` `docs/pipeline/offline-ingestion-pipeline.md` `docs/rule/hierarchical-chunking.md` `.planning/quick/260611-enhance-max-concurrency-34/` | 使用 GSD quick 将三层切片增强全局并发热更新并同步到 `34` 验证档 | T-070,T-069,T-066 | 2026-06-11 | 已通过控制台 settings DB override 确认 `HIERARCHICAL_ENHANCE_MAX_CONCURRENCY=34` 生效，并把 env 基线和切片/入库性能文档同步到 `16/3/4 + 34`；本轮只调整 enhanced 外呼全局 hard cap，不改变 prompt、模型参数、触发条件、slot 合并顺序或最终 chunk；下一轮重点看 `enhanceMaxConcurrency/enhancePeakConcurrency/enhanceWallMs/enhanceKeyThrottleCount/enhanceKeyRetryCount/enhanceFailures`，如限流或失败上升优先回退到 `26` |
| T-070 | DONE | codex | observability | `core/chunker/hierarchical.py` `tests/test_chunker.py` `docs/performance-discussions/chunk-performance.md` `.planning/quick/260610-enhance-api-key-pool/SUMMARY.md` | 为三层切片增强 key 池补充匿名 per-key 分布指标，验证 `LLM_API_KEY_POOL` 是否真实分摊请求 | T-069 | 2026-06-11 | 新增 `enhanceLlmKey.llm-key-N.calls/successes/failures/throttles/totalMs` 与 `enhanceVlKey.vl-key-N.*` 扁平数字指标，不记录真实 key，不改变 prompt、模型参数、并发 cap、slot 合并顺序或最终 chunk；`python -m py_compile core/chunker/hierarchical.py`、`pytest tests/test_chunker.py -q`、`pytest tests/test_backend_app.py -q`、`pytest tests/test_api_console.py tests/test_runtime_settings.py -q` 通过 |
| T-069 | DONE | codex | feature | `core/chunker/hierarchical.py` `core/runtime_settings.py` `.env.example` `.env.docker.example` `docs/performance-discussions/` `.planning/quick/260610-enhance-api-key-pool/` | 使用 GSD quick 为三层切片增强增加多 API key 池，先解决切片增强吞吐优化 | T-066,T-063,T-058 | 2026-06-10 | 已新增 `LLM_API_KEY_POOL` 与 `VL_API_KEY_POOL`，在不改变 prompt、模型参数、触发条件、slot 合并顺序或最终 chunk 结果的前提下，为 text/table/fragment 与 image/VL 增强提供同源 key 池；仅明确限流错误触发 key 冷却和换 key 重试，新增 `enhanceLlmKeyPoolSize/enhanceVlKeyPoolSize/enhanceKeyThrottleCount/enhanceKeyRetryCount/enhanceKeyCooldownCount` 等指标；解析和向量化仍作为独立优化线跟踪；`pytest tests/test_chunker.py -q`、`pytest tests/test_api_console.py tests/test_runtime_settings.py -q`、`pytest tests/test_backend_app.py -q` 通过 |
| T-068 | DONE | codex | ops | `redis` `backend/services/task_store.py` `data/logs/66d01060-7351-4434-a564-69a4d777b441.log` `.planning/quick/260610-mark-invalid-dm-task-failed/` | 使用 GSD quick 释放中医学-DM 最新一次无效入库任务，并标记为失败 | T-067,T-064 | 2026-06-10 | 目标任务 `66d01060-7351-4434-a564-69a4d777b441`，知识库 `zhong-yi-xue-dm-172b`，文件 `36.中医学 第9版.pdf`；本地日志停在 `chunk` 增强 `1/958`，API 显示仍为 `running`；已通过 Redis/backend task_store 写回 `status=failed`、`done=true`、`awaiting_confirmation=false`、`chunk.status=failed`，并保留任务 TTL；`GET /api/ingestion/tasks/{id}` 与任务列表已复核为失败态 |
| T-067 | DONE | codex | bugfix | `backend/services/task_store.py` `tests/test_task_store.py` `.planning/quick/260610-ingestion-task-order-newest-first/` | 使用 GSD quick 调整文档入库任务队列排序，按创建时间最新优先展示 | T-064 | 2026-06-10 | `load_all_tasks()` 已从按 UUID 倒序改为按 `created_at` 倒序，缺失时回退 `updated_at/id`；Redis 与内存降级路径共用排序逻辑；`pytest tests/test_task_store.py -q`、`pytest tests/test_backend_app.py -q` 通过；已重建 backend 镜像和容器，在线 `/api/ingestion/tasks` 确认最新任务在最上方，`/api/health` 返回 ok |
| T-066 | DONE | codex | ops | `.env` `.env.example` `.env.docker.example` `core/chunker/hierarchical.py` `core/runtime_settings.py` `docs/performance-discussions/` `docs/pipeline/` `docs/rule/` `.planning/quick/260610-enhance-concurrency-22-and-track-split/` | 使用 GSD quick 将三层切片严格等价增强验证档提升到 `16/3/4 + 22`，并补齐解析、切片、向量化三线跟踪总览 | T-065,T-063,T-058,T-057 | 2026-06-10 | 基于真实任务 `5bc9f5f4-c66c-4f37-a4f6-b225a4bad022`：`chunkBaseMs=233`、`enhanceWallMs=248692`、`enhanceTasks=958`、`enhanceFailures=0`、峰值并发 18；本轮只提高 enhanced 外呼并发预算，不改变 prompt、触发条件、模型参数、slot 合并顺序或最终 chunk 结果；`pytest tests/test_chunker.py -q`、`pytest tests/test_api_console.py tests/test_runtime_settings.py -q`、`pytest tests/test_backend_app.py -q` 通过；已重建 backend 并确认运行时 `parallel_ordered/16/3/4/22/true/batch=10/embedding=8/parser=4` 生效，`/api/health` 返回 ok |
| T-065 | DONE | codex | docs | `backend/services/ingestion_service.py` `tests/test_backend_app.py` `docs/performance-discussions/` `docs/pipeline/README.md` `.planning/quick/260610-parse-observability-stage-docs/` | 使用 GSD quick 补齐解析阶段观测，并将解析、切片、向量化性能独立文档跟踪 | T-064,T-055,T-058,T-057 | 2026-06-10 | 新增 `_ParseStageTracker`，根据 parser 现有日志推进 parse stage progress，并记录 `provider/shardCount/completedShards/pollCount/parseWallMs/outputBlocks`；新增 `parse-performance.md`、`chunk-performance.md`、`embedding-performance.md`；`python -m py_compile backend/services/ingestion_service.py` 与 `pytest tests/test_backend_app.py -q` 通过 |
| T-064 | DONE | codex | bugfix | `backend/services/ingestion_service.py` `frontend/src/components/knowledge-base/ingestion-workspace.tsx` `frontend/src/lib/contracts/types.ts` `tests/test_backend_app.py` `.planning/quick/260610-ingestion-progress-visibility/` | 使用 GSD quick 修复入库任务运行中控制台进度不更新的问题 | T-063,T-036 | 2026-06-10 | 根因为后端任务 payload 未暴露 `stages[].progress`，切片进度回调只更新 message，SSE 只按 stage 变化推送，前端也未把 SSE progress/message 合并到任务卡片；已补 stage progress、三层切片消息到百分比映射、SSE 快照推送、前端当前阶段进度条与回归测试；已重建 backend/frontend，容器内确认 `_task_to_payload` 包含 `progress`，`/api/health` 返回 ok |
| T-063 | DONE | codex | ops | `core/chunker/hierarchical.py` `.env` `.env.example` `.env.docker.example` `core/runtime_settings.py` `backend/services/console_service.py` `docs/pipeline/` `docs/rule/` `docs/performance-discussions/` `.planning/quick/260610-dynamic-enhancement-borrowing/` | 使用 GSD quick 将三层切片增强固定池升级为动态借用调度，继续提升真实教材切片性能 | T-062,T-058 | 2026-06-10 | 保持 `parallel_ordered` 严格等价口径，不改变 prompt、触发条件、模型参数、slot 合并顺序或最终 chunk 结果；当前配置为 `text/table/image=12/3/4` 软上限 + `HIERARCHICAL_ENHANCE_MAX_CONCURRENCY=18` 全局硬上限，空闲池容量可被剩余任务类型借用；已重建 backend 并确认运行时 `parallel_ordered/12/3/4/18/true/batch=10/embedding=8` 生效，`/api/health` 返回 ok；`pytest tests/test_chunker.py -q`、`pytest tests/test_api_console.py tests/test_runtime_settings.py -q`、`pytest tests/test_backend_app.py -q` 通过 |
| T-062 | DONE | codex | ops | `.env` `.env.example` `.env.docker.example` `docs/pipeline/` `docs/rule/` `.planning/quick/260610-efq-text-table-image-12-2-4-embedding-8/` | 使用 GSD quick 继续外呼并发验证：三层增强提升到 `text/table/image=12/2/4`，embedding 并发提升到 `8` | T-061,T-060,T-057,T-058 | 2026-06-10 | 已保持 `parallel_ordered` 与严格等价口径，只提升增强与向量化并发；已重建 backend 并用 `printenv` 确认运行时 `parallel_ordered/12/2/4/true/batch=10/embedding=8/retries=2/parser=4` 生效，`/api/health` 返回 ok；下一轮重点观测 `enhanceWallMs/enhanceImageMs/enhanceFailures/embeddingWallMs/retryCount` |
| T-061 | DONE | codex | ops | `.env` `.env.example` `.env.docker.example` `docs/pipeline/` `docs/rule/` `.planning/quick/260610-d8g-text-table-image-10-2-3/` | 使用 GSD quick 将三层切片增强并发提升到 `text/table/image=10/2/3`，准备下一轮真实教材切片性能验证 | T-060,T-058 | 2026-06-10 | 已保持 `parallel_ordered` 与严格等价口径，只把文本增强 worker 调到 10、图片/VL worker 调到 3、表格保持 2；已重建 backend 并用 `printenv` 确认运行时 `parallel_ordered/10/2/3/true` 生效；下一轮重点观测 `enhanceWallMs/enhanceImageMs/enhanceFailures` |
| T-060 | DONE | codex | ops | `.env` `.env.example` `.env.docker.example` `docs/pipeline/` `docs/rule/` `.planning/quick/260610-cjk-ingestion-concurrency-validate/` | 使用 GSD quick 按低风险档继续提升入库链路并发配置，准备下一轮真实教材性能验证 | T-055,T-057,T-058 | 2026-06-10 | 已将 Document Mind shard 并发调为 4、三层增强 text/table/image 调为 8/2/2、embedding batch size 固定 10 且并发调为 6；已重建 backend 并用 `printenv` 确认运行时生效；下一轮重点观测 `enhanceImageMs/enhanceFailures/embeddingWallMs/retryCount` |
| T-055 | DONE | codex | feature | `.planning/phases/10-06b-document-mind-sharded-cloud-parse/` `core/parser/` `docs/pipeline/` | 执行 10-06b：给阿里 Document Mind 增加大文件自动分片解析，超过 150MB 或 50 页时分片并发解析，按原始全局页码合并后继续入库 | T-053,T-051 | 2026-06-09 | 已抽 `core/parser/pdf_sharding.py`，让 MinerU 与 Document Mind 共用 PDF 体检、分片、页码 offset 和合并逻辑；已实现 Document Mind 自动分片、并发解析、全局 `page_idx` 合并、原始 `source_file` 保留、控制台/env 配置与文档；`pytest tests/test_pdf_sharding.py tests/test_document_mind_parser.py tests/test_parser_provider.py tests/test_cloud_parser.py tests/test_backend_app.py tests/test_api_console.py tests/test_runtime_settings.py -q` 92 passed |
| T-059 | DONE | codex | docs | `README.md` `docs/` `.planning/` `todo.md` | 根据 2026-06-09 当前实现进度整体同步项目总览、链路、架构、迭代、性能路线与 planning 文档，明确 10-08b 已完成、10-09 待执行 | T-058,T-057 | 2026-06-09 | 仅更新文档，不改项目代码；同步 MinerU/Document Mind provider 切换与分片、图谱预览、可控入库、三层切片严格等价性能优化、10-09 向量化性能计划等当前口径 |
| T-053 | DONE | codex | feature | `.planning/phases/10-06-parser-provider-switch/` `core/parser/` `backend/services/` `docs/pipeline/` | 规划并执行解析 Provider 可切换：保留 302AI MinerU，新增阿里 Document Mind POC，通过 `PDF_PARSER_PROVIDER` 配置切换对比 | T-051 | 2026-06-08 | 已新增 provider 选择层、Document Mind 官方 OpenAPI 调用、控制台配置项与 env 示例；不使用 mock 解析结果或临时 URL 桩，不启用自动 fallback；相关 parser / backend settings 回归通过 |
| T-051 | DONE | codex | feature | `.planning/phases/10-05-mineru-sharded-cloud-parse/` `core/parser/` `backend/services/` `docs/pipeline/` | 执行 10-05 MinerU 云端大文件分片解析优化：PDF 体检、按页拆 shard、多 worker 解析、全局页码合并并同步完整链路文档 | T-036 | 2026-06-05 | 已落地 parser 自动分片、入库统一入口、sharding 配置、控制台解析配置展示、完整链路文档；`pytest tests/test_cloud_parser.py -q` 29 passed，`pytest tests/test_api_console.py tests/test_runtime_settings.py tests/test_backend_app.py -q` 31 passed |
| T-026 | CANCELLED | shared | planning | `.planning/` | 规划下一阶段：多租户 / 监控告警 / CD 自动部署 | - | 2026-06-03 | 已被基于 FEATURE-AUDIT 的 v6.0 主线计划替代；上述方向回退到远期候选 |
| T-036 | DOING | gsd | planning | `.planning/` `.planning/phases/10-01-ingestion-control/` `backend/` `core/` `frontend/` | 执行 v6.0 主线 10-01：已落地设置 DB override、MinerU 参数扩展、切片草稿预览/编辑/确认主链路，继续推进 phase 收口与 UAT | T-033,T-035 | 2026-06-03 | 已补 `console_settings` / `chunk_drafts` schema、`awaiting_confirmation` 分段入库、settings 页来源升级、单库 ingestion 草稿确认区，以及草稿分批显示与批量删除 |
| T-057 | DONE | codex | planning | `.planning/phases/10-09-embedding-performance-optimization/` `core/embedding/` `core/rag/` `core/output/` | 执行 10-09：向量化性能优化，新增入库 embedding 有界并发、query embedding 进程内 TTL cache，并评估 `executemany -> execute_values` 写库优化 | T-056 | 2026-06-09 | 已完成：`embed_texts()` 保持返回类型并改为有界并发 batch，默认 `LLM_EMBEDDING_MAX_CONCURRENCY=4`；新增 batch retry 与 embedding stage metrics；普通/Graph RAG query embedding 进程内 TTL cache；chunks 写库优先 `execute_values` 并保留 fallback；`pytest tests/test_ingestion_embedding_metrics.py tests/test_embedding_client.py tests/test_pgvector_writer.py tests/test_rag_retriever.py tests/test_graph_retriever.py tests/test_backend_app.py tests/test_api_console.py -q` 58 passed |
| T-058 | DONE | codex | feature | `.planning/phases/10-08b-hierarchical-hotpath-equivalence/` `core/chunker/` `docs/pipeline/` `docs/rule/` | 执行 10-08b：三层切片严格等价热路径优化，继续降低大教材切片阶段固定开销 | T-056 | 2026-06-09 | 已新增线程内 OpenAI 兼容 client 复用开关 `HIERARCHICAL_REUSE_LLM_CLIENTS`，优化 `link_related_chunks()` relation 去重热路径，并补充 `enhanceTasks/enhanceFailures/enhanceClientReuse` 等 chunkTimings；不改变 prompt、触发条件、slot 合并或最终 chunk 结果；`pytest tests/test_chunker.py tests/test_linker_typed.py tests/test_enhanced_extraction.py tests/test_backend_app.py tests/test_api_console.py -q` 65 passed |
| T-050 | DONE | codex | feature | `.planning/phases/10-04b-graph-relation-views/` `frontend/` `docs/pipeline/` | 执行 10-04b 文档图谱关系视图与图例增强：把“切片关系”和“实体关系”拆成视图层级并补图例 | T-049 | 2026-06-05 | 已落地图谱三视图、中文 edge label、节点/边图例、可见/隐藏数量提示和自适应默认视图并同步文档；`frontend npm run typecheck` 通过；未改变图谱 API shape |
| T-049 | DONE | codex | planning | `.planning/phases/10-04a-document-graph-preview/` `.planning/ROADMAP.md` `backend/` `frontend/` `docs/pipeline/` | 执行 10-04a 文档详情知识图谱预览：在文档详情内展示当前文档的 chunk/entity/relation/triple 图谱 | T-036,T-046 | 2026-06-04 | 已新增文档级图谱 API、详情页“知识图谱”视图、Cytoscape 预览组件和管线文档；`pytest tests/test_document_graph_service.py tests/test_backend_app.py -q` 22 passed，`npm run typecheck` 通过 |
| T-040 | DONE | codex | bugfix | `frontend/` `backend/` `bug.md` `.planning/phases/10-01-ingestion-control/` | 收口 10-01 最后 UAT 缺口：API 序列已验证 settings roundtrip + 草稿编辑/合并/删除/confirm 成功，但浏览器级 confirm 仍未形成稳定闭环，同时补齐 settings 敏感字段脱敏与容器 embedding 出网排查 | T-036 | 2026-06-04 | 已补 settings 脱敏、Redis 不可用时任务存储内存降级、RAG 空库 BM25 除零保护；`pytest` 新增任务存储 / HTTP 代理 / 空库检索回归通过，`frontend npm run typecheck` 通过；容器级 `redis+backend` 重建后 `/api/health`、`/api/console/overview-metrics`、`/api/rag/query`、`/api/rag/graph-query` 均返回 200，embedding 探针成功返回 `1 x 1024` |
| T-031 | DONE | codex | feature | `frontend/` `.planning/workstreams/next/` | 补全全局页与单库工作台的导航回路，增加最近访问知识库上下文保持，并同步 GSD 后续计划 | T-030 | 2026-06-02 | 新增 PLAN-06-06，覆盖 overview / ingestion / query / evaluation / settings 回跳入口 |
| T-032 | DONE | codex | feature | `backend/` `frontend/` `.planning/workstreams/next/` | 推进单库评测与入库过滤支持：入库任务与评测记录均已支持按知识库过滤，单库页已接入真实数据 | T-031 | 2026-06-02 | 对应 PLAN-06-07；后端记录写入 + `/api/console/evaluations?kb_id=` + 前端单库评测页/总览页传参均已落地 |
| T-030 | DONE | codex | feature | `frontend/` | 借鉴 FastGPT 的知识库前端功能划分，重组控制台导航与知识库工作台，仅调整前端信息架构 | - | 2026-06-02 | 首页切到知识库入口，单库工作台保留文档 / 入库 / 问答 / 评测闭环 |
| T-033 | DONE | gsd | planning | `.planning/workstreams/graphrag/` `docs/rule/` `docs/eval/` | GSD 接管 v5.0 知识图谱化与 Graph RAG 演进：7 个 phase（09-01~07），Graph RAG 完整链路 | - | 2026-06-02 | 已完成 `09-01`~`09-07` 当前范围闭环；补齐实体层、流程/因果链、Graph 检索与离线评测基础框架，并生成 `SUMMARY-09-01-07.md` |
| T-034 | DONE | codex | docs | `.planning/REQUIREMENTS.md` `AGENTS.md` `.codex/skills/` | 补齐 v5.0 最后文档短板：将项目需求口径从早期 PDF/CSV 验证版同步到当前 Graph RAG 里程碑，并修正文档入口过期路径 | T-033 | 2026-06-02 | 重写 `.planning/REQUIREMENTS.md` 为 v5.0 口径，同时修复共享文档路径漂移 |
| T-035 | DONE | codex | planning | `.planning/` `AGENTS.md` `docs/agent/` | 将所有 GSD 工作流收拢到项目主线：项目级状态、路线图、操作手册统一改为主线 milestone 驱动，workstream 仅作历史归档 | T-034 | 2026-06-02 | 已同步主线 requirements、milestone summary、历史 milestone/workstream 根文档、早期 `.planning/phases/` 归档与 milestone 归档命名规范；保留 `workstreams/**` 目录供溯源，但不再作为新的默认推进入口 |

---

## Backlog

| ID | 状态 | Owner | 类型 | 范围 | 任务 | 依赖 | 更新时间 | 备注 |
|----|------|-------|------|------|------|------|-----------|------|
| T-170 | DONE | codex | feature | `backend/services/ai_base_sso_service.py` `backend/routes/identity.py` `core/runtime_settings.py` `.env*` `tests/test_ai_base_sso.py` | 将 `users/{userId}` 与 `delta` 接口做成正式 HTTP 同步能力：根据服务端配置调用 `/ai/system/internal/identity/snapshot/users/{userId}?tenant_id=` 与 `/ai/system/internal/identity/snapshot/delta?last_sync_at=`，落库 `max_updated_at` 并支持首次全量、增量、重试和 `deleted` 数组处理 | T-169 | 2026-06-24 | 已实现当前用户刷新、HTTP delta 同步、`max_updated_at` / `snapshot_version` 水位记录、失败 run 记录和 `deleted` 处理；验证覆盖 SSO、identity sync、DB init、console、backend 与身份访问测试 |
| T-171 | DOING | codex | feature | `backend/` `frontend/` `docs/product/phase11-gap-analysis.md` | 在正式 SSO session 基础上进一步收口生产访问边界：限制生产环境 `X-KB-*` 请求头兜底，补快照新鲜度强制窗口、访问拒绝审计、AI 基座单点退出回调和历史知识库归属迁移检查 | T-170 | 2026-06-24 | T-176 已补旧请求头兜底开关、AI 基座 HTTP delta 调度状态接口和无授权上下文 `skipped` 兜底；2026-06-26 已补 `KB_IDENTITY_SNAPSHOT_MAX_AGE_SECONDS`、正式 SSO 高风险操作快照新鲜度门禁、`access.denied` 审计和 AI 基座 `logout-callback` 批量撤销本地 session；真实 SSO `superManager` 已触发 `http_delta`，并已按 AI 基座约定清空身份快照后用首次水位 `2000-01-01 00:00:00` 重拉成功，最终运行库为 tenants=52、users=69278、roles=227、user_roles=70755，最新成功 run 记录 payload 计数 users=69278、roles=227、user_roles=70805、deleted=91；已将 delta HTTP timeout 调整为 300 秒并补脱敏结构诊断日志与 `source_schema` 摘要；剩余失败重试和历史知识库归属迁移检查 |
| T-172 | TODO | codex | feature | `backend/routes/` `core/db/` `frontend/` | 依赖正式 SSO 的子资源权限收口：文档详情、源文件下载、CSV 导出、知识库/文档图谱、入库任务 SSE、草稿确认、评测记录、日志、配置和 API Key 管理全部按所属知识库和当前身份裁剪 | T-171 | 2026-06-23 | 继续承接 Phase 11 P0，防止只在知识库列表或 RAG 查询入口做点状守门 |
| T-165 | DONE | codex | feature | `backend/services/ai_base_sso_service.py` `backend/routes/identity.py` `core/runtime_settings.py` `.env*` `tests/test_ai_base_sso.py` | 对齐 AI 基座租户端 SSO code 协议：launch 带 `client_id`，exchange 解包 `Result.data` 并映射 `success/code/msg` 错误 | T-145,T-164 | 2026-06-23 | 2026-06-24 已进一步收敛为 RAG `/api/auth/ai-base/launch` 统一生成 state/cookie，再 302 到 AI 基座浏览器 `/sso`；`AI_BASE_SSO_BASE_URL` 仅用于后端 exchange / snapshot / delta |
| T-166 | DONE | codex | feature | `backend/` `core/db/identity.py` `scripts/` `tests/` | 将身份同步从早期 MySQL 样本脚本推进到 AI 基座 HTTP 接口：实现当前用户刷新 `/ai/system/internal/identity/snapshot/users/{userId}` 与增量 `/delta?last_sync_at=`，保存 `max_updated_at` 水位并处理 `deleted` 数组 | T-165 | 2026-06-24 | 已由 T-170 覆盖完成；MySQL 样本脚本保留为早期联调工具，生产主路径改为 AI 基座 HTTP snapshot / delta |
| T-004 | TODO | shared | planning | `.planning/` | 基于当前主线完成状态，决定是否启动下一 milestone / 新 phase | T-001 | 2026-05-29 | 需要确认是走 v5.x Graph RAG 增强，还是切到下一条主线 |
| T-005 | DONE | codex | docs | `docs/agent/` `docs/document-map.md` | 补一份 `Codex onboarding` 简版入门文档 | T-002 | 2026-06-26 | 已新增 `docs/agent/codex-onboarding.md`，覆盖先读什么、任务入口选择、编辑原则、验证收尾和常见误区；同步 `docs/agent/README.md` 与 `docs/document-map.md`。 |
| T-006 | TODO | shared | quality | repo | 在真实开发中验证共享账本是否足够，必要时再追加 `decision-log` 而不是扩大共享面 | T-002,T-003 | 2026-05-29 | 先观察，不立即新增更多共享文件 |
| T-056 | DONE | codex | feature | `.planning/phases/10-08-hierarchical-async-enhancement/` `core/chunker/` `backend/services/ingestion_service.py` `tests/test_chunker.py` | 执行三层切片严格等价性能优化第一阶段 | T-046,T-055 | 2026-06-09 | 已落地入库前 `parallel_ordered` enhanced、有序 slot 合并、`serial` 回退、切片子阶段耗时 breakdown 与聚合进度；不改变 parent/child/enhanced 原则、触发条件、prompt 或正式入库结果；enhanced cache 因严格等价口径暂延后为显式可选模式；`pytest tests/test_chunker.py tests/test_backend_app.py tests/test_api_console.py tests/test_generator.py tests/test_rag_retriever.py -q` 69 passed，`frontend npm run typecheck` 通过 |

---

## Recently Done

| ID | 状态 | Owner | 类型 | 范围 | 任务 | 依赖 | 更新时间 | 备注 |
|----|------|-------|------|------|------|------|-----------|------|
| T-054 | DONE | codex | feature | `backend/` `frontend/` `tests/` | 知识库新增编辑功能：支持编辑知识库名称、描述及默认切片策略，并在单库入库上传时默认使用该策略 | - | 2026-06-08 | 新增 `PUT /api/knowledge-bases/{kb_id}`、`knowledge_bases.default_strategy` 兼容迁移、前端编辑弹窗与策略展示；`pytest tests/test_backend_app.py -q`、`frontend npm run typecheck` 通过 |
| T-052 | DONE | codex | feature | `backend/` `frontend/` `tests/` | 实现知识库级图谱：新增 KB 聚合图谱 API，并在单库总览页复用图谱组件展示切片/实体/三元组预览 | T-049,T-050 | 2026-06-05 | 新增 `/api/knowledge-bases/{kb_id}/graph`、`getKnowledgeBaseGraph()`、单库“知识库图谱”区域；聚合节点保留真实 documentId/filename；`pytest tests/test_document_graph_service.py tests/test_backend_app.py -q` 27 passed，`npm run typecheck` 通过 |
| T-048 | DONE | codex | bugfix | `core/rag/` `backend/adapters/` `docs/pipeline/` `tests/` | 优化教材图表编号定位查询：`图/表 + 编号` 走 media_ref 直达召回，避免完整 RAG 链路导致分钟级耗时 | T-046 | 2026-06-04 | 命中后跳过 embedding、BM25、rerank API 与 LLM 生成，直接返回定位答案、引用和候选；已同步在线召回/在线 RAG 文档，`pytest tests/test_rag_retriever.py tests/test_rag_adapter.py tests/test_backend_app.py -q` 与前端 typecheck 通过 |
| T-047 | DONE | codex | feature | `backend/` `core/runtime_settings.py` `frontend/` `tests/` `redis` | 优化系统配置与评测文案，并清理历史入库任务脏状态 | T-036 | 2026-06-04 | 模型配置已按通用/向量/清洗/切片/质量审核/三层增强/问答生成拆分，系统提示词补内置默认并使用多行编辑；评测“拒答”改为“资料不足，未回答”；已从 Redis 清理 19 个历史 pending/running/awaiting_confirmation 入库任务，活跃任务归零 |
| T-046 | DONE | codex | feature | `core/rag/` `core/chunker/` `backend/` `frontend/` `tests/` | 对齐三层切片在线召回口径：默认检索 child+enhanced，enhanced 命中回指 child，parent 仅作结构导航；补图/表编号引用关系与规范文档名/位置展示 | T-044,T-045 | 2026-06-04 | 已修复 `图1-3-3-6` / `表1-3-3-1` 这类教材引用到媒体切片的 `refers_to` 关系；RAG citations/candidates 优先显示 `documents.filename` 与 `P.x · #n`；`pytest tests/test_chunker.py tests/test_rag_retriever.py tests/test_generator.py tests/test_reranker.py -q`、相关 RAG/API 测试和前端 typecheck 通过 |
| T-045 | DONE | codex | fix | `backend/` `core/db/` `tests/` `bug.md` | 修复文档详情图片切片不可见：草稿确认链路保留 `image_path`，并把 `/api/assets/output/**` 改为 app 级静态文件挂载 | T-043 | 2026-06-04 | 根因为 `chunk_drafts` 未保存图片路径且静态资源挂在 `APIRouter` 上返回 404；已补 schema / 保存 / 读取 / 确认链路、静态资产测试，并为当前 `带表格的教材片段.pdf` 回填 5 个图片路径；#3 图片 URL 已返回 200 |
| T-044 | DONE | codex | fix | `core/rag/` `backend/` `tests/` `bug.md` | 修复在线问答 `/api/rag/query` 在真实知识库召回时反复 503：BM25 scores 为数组对象时不再触发布尔判断异常 | T-040 | 2026-06-04 | 根因为 `rank_bm25` 返回 `numpy.ndarray`，`_sparse_retrieve()` 的 `if not scores` 抛 `ValueError`；已改为先转 `list[float]`，补 numpy-like 回归测试；`pytest tests/test_rag_retriever.py tests/test_backend_app.py -q` 20 passed；重建 backend 后 `kb_id=test-9e68` 的 `/api/rag/query` 返回 200 |
| T-043 | DONE | codex | feature | `backend/` `core/` `frontend/` `tests/` | 补齐图片切片链路：解析、草稿、确认入库、检索与文档详情保留并展示图片路径。 | T-036 | 2026-06-04 | 历史条目由乱码占位恢复为摘要描述；核心结果为 `chunks.image_path` 贯穿入库与展示链路，并通过 `pytest tests/test_backend_app.py tests/test_document_export_service.py tests/test_chunker.py -q` 与 `frontend npm run typecheck`。 |
| T-041 | DONE | codex | fix | `backend/` `core/` `tests/` `bug.md` | 修复文档名称丢失：解析/切片链路继续透传原始文件名，重复命中相同文档 hash 时同步刷新 `documents.filename`，避免文档列表长期显示临时/旧文件名 | T-040 | 2026-06-04 | 已补 `parse_pdf_from_url(..., original_name=...)` 透传、`write_to_pgvector()` 重复文档名称刷新，以及 `pytest tests/test_cloud_parser.py tests/test_pgvector_writer.py tests/test_backend_app.py -q` 回归通过；现存 `test-9e68` 库中的 `phase-10-01-*` 记录为历史探针数据，未在本次修复中自动删除 |
| T-042 | DONE | codex | ops | `docker-compose.yml` `.env.docker.example` | 移除容器侧 `host.docker.internal:7890` 代理透传：`db-init`、`backend`、`cli` 在 compose 中显式将 `HTTP_PROXY/HTTPS_PROXY/NO_PROXY` 及其小写变量置空，同步删除 Docker 环境模板中的 7890 示例，避免国内 302 地址场景继续走宿主机代理 | T-040 | 2026-06-04 | 按当前部署口径保留国内镜像源与 `302AI_API_BASE=https://api.302ai.cn`，并覆盖 `.env` 旧代理值对容器的继承；未改动宿主机本地进程的 HTTP 客户端兜底逻辑 |
| T-039 | DONE | codex | feature | `backend/` `frontend/` `tests/` `docs/` | 为正式入库后的文档补齐 CSV 导出：新增 `/api/documents/{document_id}/export.csv`，并在文档列表接入下载按钮 | T-036 | 2026-06-03 | 导出基于 `documents/chunks/chunk_relations/kg_triples`，默认 UTF-8 BOM；`pytest tests/test_backend_app.py tests/test_document_export_service.py -q` 与 `frontend npm run typecheck` 通过；README / pipeline / architecture 已同步 |
| T-038 | DONE | codex | fix | `backend/` `frontend/` `tests/` | 收口 10-01 配置持久化链路：修复 settings 保存遇到数据库异常时静默成功，并同步前端来源标记与回归测试 | T-036 | 2026-06-03 | `PUT /api/console/settings` 持久化失败现返回 503；settings 页保存后即时显示 `db` 来源；`pytest tests/test_api_console.py tests/test_runtime_settings.py -q` 通过 |
| T-037 | DONE | codex | fix | `backend/` `core/` `tests/` | 修复本地 HTTP 模式下知识问答 / Graph RAG 503：补后端 `.env` 加载并兜住 Docker 专用代理误继承，打通外部模型与解析链路 | - | 2026-06-03 | 新增 `core/http_client.py` 并接入问答、召回、解析、清洗、切片；`pytest tests/test_backend_app.py tests/test_http_client.py -q` 17 passed，本地复测 `/api/rag/query` 与 `/api/rag/graph-query` 返回 200 |
| T-000 | DONE | shared | feature | `backend/` `src/api/` `README.md` | 完成后端边界重构、CLI/HTTP 解耦和文档同步 | 08-01~08-04 | 2026-05-29 | 来自 GSD Phase 8 执行结果 |
| T-001 | DONE | shared | feature | `.planning/workstreams/next` | 完成 v2.0 `next` workstream Phase 8 服务端规范化，并收敛 backend/app/router/schema/service 边界 | 08-01~08-04 | 2026-05-29 | 已在 `.planning/workstreams/next/STATE.md` 标记 phase_complete |
| T-002 | DONE | codex | docs | `AGENTS.md` `.codex/` `docs/` | 建立基于 Codex + GSD 的项目级配置，替代对 ECC 的依赖 | - | 2026-05-29 | 已完成主说明、技能、共享规则、命令手册 |
| T-003 | DONE | codex | docs | `todo.md` `bug.md` `docs/` | 将共享账本从模板推进到可持续维护状态，并补审查手册 | T-002 | 2026-05-29 | 已完成 `todo.md` 初始化与 `codex-review-playbook` |
| T-007 | DONE | codex | feature | `frontend/` `backend/services/` | 将控制台与 API 回退文案改为默认中文输出，并补轻量中文映射层 | - | 2026-05-29 | 已覆盖 mock/live 两种模式下的主要用户可见文案 |
| T-017 | DONE | backend-dev | feature | `backend/routes/` | Phase 06-04：知识库 CRUD + 文件上传 + SSE + 重试 + 仪表盘统计（7个端点） | - | 2026-05-31 | 新增 ingestion.py / dashboard.py / ingestion_service.py |
| T-021 | DONE | frontend-dev | feature | `frontend/` | Phase 06-04：前端接入真实 API（overview/knowledge-bases/ingestion 改为 Client Component） | T-017 | 2026-05-31 | TypeScript 零错误，next build 通过 |
| T-024 | DONE | qa-tester | fix | `tests/` | pytest 13 个历史失败修复（src.* → core.*），69 passed 0 failed | - | 2026-06-01 | 5 个测试文件批量替换 patch 路径 |
| T-025 | DONE | frontend-dev | feature | `frontend/` | Phase 07-01：/query /evaluation /settings 改为 Client Component 接入真实 API | - | 2026-06-01 | tsc 零错误，next build 通过 |
| T-023 | DONE | backend-dev | feature | `backend/services/` | Phase 07-02：真实 MinerU 管道接入，INGESTION_MOCK 环境变量开关 | - | 2026-06-01 | run_pipeline_real，asyncio.to_thread，tempfile 清理 |
| T-022 | DONE | backend-dev | feature | `backend/services/` `docker-compose.yml` | Phase 08-01：Redis 任务持久化（task_store.py，降级内存模式） | - | 2026-06-01 | redis>=5.0 依赖，docker-compose redis service |
| T-027 | DONE | qa-tester | feature | `.github/workflows/` | Phase 08-02：GitHub Actions CI/CD（backend-ci / frontend-ci / docker-build） | - | 2026-06-01 | 三条 workflow，GHA cache |
| T-028 | DONE | codex | fix | `backend/` `frontend/` `README.md` `.env*` `.github/workflows/` | 移除运行时 mock / 假数据回退，统一改为真实数据或显式失败，并补任务详情真实字段与前端错误展示 | - | 2026-06-01 | pytest 12 passed，frontend typecheck 通过 |
| T-029 | DONE | codex | fix | `backend/services/` `core/parser/` `frontend/` | 将 OSS 上传与 MinerU 云解析拆分为独立阶段，并补充结果 ZIP 下载诊断日志，定位容器连接拒绝问题 | T-028 | 2026-06-01 | pytest 76 passed，frontend typecheck 通过 |

---

## Status Enum

- `TODO`
- `DOING`
- `BLOCKED`
- `DONE`
- `CANCELLED`

## Owner Enum

- `human`
- `codex`
- `claude`
- `gsd`
- `shared`
