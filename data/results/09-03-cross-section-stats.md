# 09-03 Cross-section Stats

## 状态

本文件作为 `09-03` 跨章节语义边统计报告的归档路径。

## 当前结论

- 代码层已具备：
  - `semantic_similar`
  - `duplicate_of`
  - 同 parent 跳过
  - `enabled` 开关
  - `topk` / `threshold` / `dup_threshold`
- 本轮未运行真实教材样本，因此尚未产出“跨章节边占比”的实测统计

## 后续补全方式

在真实样本执行时补充：

1. 新增边总数
2. 跨 parent 边数及占比
3. `duplicate_of` 数量
4. 抽检案例与阈值说明
