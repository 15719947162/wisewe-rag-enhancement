"""
Backend - HTTP 服务应用层

本包是整个 RAG 系统的 HTTP 服务层，负责对外暴露 REST API 接口。
可以把它理解为一个"服务员"，接收用户请求，调用核心能力（core/）处理，然后返回结果。

## 架构定位

项目采用分层架构，依赖方向为：backend → core（永远不要反过来）

```
┌─────────────────────────────────────────────────────────────┐
│                    前端 / 外部调用方                          │
└─────────────────────────────────────────────────────────────┘
                              ↓ HTTP 请求
┌─────────────────────────────────────────────────────────────┐
│  backend/                    ← 你在这里（HTTP 服务层）         │
│  ├── routes/     路由层：定义 API 端点                        │
│  ├── schemas/    数据模式：请求/响应的数据结构定义              │
│  ├── services/   服务层：编排业务逻辑                          │
│  └── adapters/   适配器：桥接 backend 和 core                 │
└─────────────────────────────────────────────────────────────┘
                              ↓ 函数调用
┌─────────────────────────────────────────────────────────────┐
│  core/                       核心领域能力（无 HTTP 依赖）       │
│  ├── parser/     PDF 解析                                    │
│  ├── cleaner/    内容清洗                                     │
│  ├── chunker/    切片策略                                     │
│  ├── embedding/  向量化                                       │
│  ├── rag/        RAG 检索生成                                 │
│  └── ...                                                     │
└─────────────────────────────────────────────────────────────┘
```

## 启动服务

```bash
# 方式一：直接运行
python backend/serve.py

# 方式二：使用 uvicorn（支持热重载）
uvicorn backend.app:app --reload --port 8000
```

## 子模块说明

| 子模块 | 职责 |
|--------|------|
| routes/ | HTTP 路由定义，每个文件对应一类业务接口 |
| schemas/ | Pydantic 模型，定义请求和响应的数据格式 |
| services/ | 业务服务编排，协调多个核心模块完成复杂流程 |
| adapters/ | 适配器，将 HTTP 调用转换为对 core 模块的调用 |

## 与 CLI 的区别

- **backend/**：HTTP 服务模式，支持前后端联调、实时交互
- **main.py + backend/cli.py**：命令行模式，适合批量处理、脚本运行
"""

# backend/ 是一个纯包，没有需要导出的符号
__all__: list[str] = []

